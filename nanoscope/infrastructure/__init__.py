"""Adapters: persistence, storage, models, device, imaging, logging.

Implements the ports defined in `core.ports`. Everything that touches a file, a
network, a GPU or a third-party framework lives here — which is why torch does.
"""
