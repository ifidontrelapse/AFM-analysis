# TASKS

**Updated:** 2026-08-13 · **Active:** **M7 — Annotation & metrology tools**, with `M7-T01` current. **M6 closed 2026-08-13** — nine tasks, ADR-0061…ADR-0069, all four exit criteria. **M5 closed 2026-08-13** with all nine tasks done, ADR-0052…ADR-0060, and four of five exit criteria met — the fifth, *GUI smoke tests in CI*, is unverified rather than unmet. **M4 closed 2026-08-12** with all fifteen tasks done, all six exit criteria met and ADR-0038…ADR-0051; the golden did not move once in fifteen tasks. M3 closed 2026-08-09 with 25 of 26 tasks done and all five exit criteria met (`M3-T19` last, 2026-08-09); `M3-T16` stays open and blocked on **B6**, and **B-062**, **B-065**, **B-066**, **B-067** stay in M3 as algorithm choices needing an operator's view. Every defect the July audit reproduced is closed except **D-24** (the stale README, M9). M5 left **W10 closable rather than closed** and **B-068** open

Full task breakdown per milestone. One task ≈ one branch ≈ one focused work session.
Statuses: `[ ]` todo · `[~]` in progress · `[x]` done · `[!]` blocked · `[-]` dropped.

Task IDs are permanent. A dropped task keeps its ID; IDs are never reused.
`D-NN` references are defects from `docs/audit/2026-07-28-baseline-audit.md`.

---

## M0 — Engineering foundation ✅

| ID | Task | Status |
|---|---|---|
| M0-T01 | Analyze the existing repository; record strengths and weaknesses with evidence | [x] |
| M0-T02 | Define the target architecture and layer contracts (`Architecture.md`) | [x] |
| M0-T03 | Write the project constitution (`PROJECT_RULES.md`) | [x] |
| M0-T04 | Write initial ADRs (0001–0011) | [x] |
| M0-T05 | Break the project into milestones (`Roadmap.md`) | [x] |
| M0-T06 | Break milestones into tasks (this file) | [x] |
| M0-T07 | Establish the state/session protocol (`STATE.md`, `Progress.md`, `CURRENT_TASK.md`) | [x] |
| M0-T08 | Select the first task and record it | [x] |

---

## M1 — Repository hygiene & quality gates

| ID | Task | Detail | Status |
|---|---|---|---|
| M1-T01 | Untrack build artifacts and weights; rewrite `.gitignore` | Done 2026-08-03: 2 877 → **77** tracked files; 2 800 `node_modules` untracked; 26 MB `yolov8s-world.pt` removed from the index before it entered history; junk deleted; `plan.md` archived; `.claude/settings.json` shared. Zero golden drift. D-19 | [x] |
| M1-T02 | Declare dev dependencies | Done 2026-08-03: pytest 9.1.1, pytest-cov 7.1.0, ruff 0.16.1, mypy 2.3.0. Baseline measured — 196 ruff findings (109 in `src/`), 30 mypy errors, 1 failing test. No runtime version moved; golden stable. D-20 | [x] |
| M1-T03 | Repair the ruff configuration | Done 2026-08-03: removed `fix = true` (`ruff check` was rewriting sources), moved `select`/`ignore` to `[tool.ruff.lint]`, py311 → py312, fixed `known-first-party`, dropped dead `S101`, excluded notebooks. `src/` findings unchanged at 109 — repair only | [x] |
| M1-T04 | Add mypy configuration | Done 2026-08-04: strict for `nanoscope.*` from its first line; `src/` checked but not silenced (22 errors, 13 of them static confirmations of D-01/D-02/D-07/D-10/D-16); `ignore_missing_imports` per module. Three new defects filed as M3-T17…T19 | [x] |
| M1-T05 | Wire the characterization harness into pytest | Done 2026-08-04: `tests/characterization/test_golden.py` calls the existing runner through a new `diff_against_golden()` seam; marked `slow` (192 s), marker registered. `pytest.ini` folded into `pyproject.toml` and deleted. Failure proven by perturbing a golden value | [x] |
| M1-T06 | Replace `tests/test_io.py` | Done 2026-08-04: deleted; replaced by `tests/unit/test_afm_io.py` — 22 tests over a synthetic Nanoscope byte stream (round trip, calibration, unit conversion, 8 failure modes, npy, SEM/TEM). No binary fixture, no `data/`. **`pytest` is green.** Suite validated by killing 4 mutants of the parser; one new defect found and filed as M3-T20. D-20 | [x] |
| M1-T07 | Add pre-commit | Done 2026-08-04: 9 hooks, each demonstrated failing on a deliberately bad staged file. ruff runs as a `repo: local` hook on the project's own version, so no second version is ever declared. Rewriting hooks skip `src/` + `preprocess_batch.py` (109 findings there would block every M2 commit); refusing hooks apply everywhere. pytest/mypy stay out of the commit path → CI | [x] |
| M1-T08 | Add CI | Done 2026-08-04, **green after four runs**. Workflow written and green locally, but the first real runs on GitHub found what local verification could not. Run 1: red at pytest, no readable reason (job logs need admin) → diagnostics added, failures now go to annotations + run summary. Run 2: red on my error — `setup-uv` publishes no floating `@v9` tag; both actions now pinned exactly. Run 3: red on **one** golden difference, an exception *message*, not a number: CI resolved Python 3.14 (`requires-python = ">=3.12"`) and 3.14 reworded `too many values to unpack`. Python 3.12 pinned + asserted → **run 4 green**. Underlying fragility filed as **B-058** | [x] |
| M1-T09 | Clean notebooks | Done 2026-08-04: outputs stripped via the configured hook — **8.3 MB → 32 KB**, all 45 cells of code intact; both moved to `notebooks/` with a README; `main.ipynb` (0 bytes, invalid JSON) deleted. Tracked working tree 17 MB → **7.8 MB**. **`pre-commit run --all-files` is green for the first time.** Stale references fixed in `README.md` and `project.md` | [x] |
| M1-T10 | Add a one-command gate | Done 2026-08-04: `Makefile`, 53 lines, targets `check` `format` `lint` `test` `fast` `golden` `types` `lint-legacy`, bare `make` prints the list. **CI calls the targets** — the workflow no longer contains a copy of any command, which is the point of the task. Proven to fail closed: a deliberately misformatted file stopped `check` at step 1 in 0.04 s with exit 2, and a failing test failed the target. `types`/`lint-legacy` stay outside `check` because the legacy baseline is non-zero by design; a gate that can never pass gets bypassed. `pretty = true` dropped from `[tool.mypy]` — the target passes `--no-pretty`, so it was dead config | [x] |
| M1-T11 | Decide the fate of `.zip`, `output/`, `__pycache__` at repo root | Absorbed into M1-T01: all deleted and ignored | [x] |

---

## M2 — Domain extraction (behaviour-preserving)

> **Gate for every task in this milestone:** `python tests/characterization/capture.py`
> must report zero drift. Any drift is a bug in the move.

| ID | Task | Detail | Status |
|---|---|---|---|
| M2-T01 | Create the package skeleton | Done 2026-08-04: `nanoscope/{app,core,application,infrastructure,gui,resources}` + `py.typed`, each layer's `__init__` stating its half of the dependency rule. Distribution `afm-analysis` → `nanoscope`; `uv.lock` regenerated and the diff read — **only the project entry moved, 0 of 119 dependency versions changed**, which matters because CI runs `uv sync --locked` and the golden is version-sensitive. mypy now checks 20 files instead of 13 and the strict `nanoscope.*` override **binds for the first time** (M1-T04 wrote it before the package existed; it had been reported unused ever since). No sub-packages below the layer level — those arrive with the code in M2-T02…T08. Zero code moved, golden zero drift | [x] |
| M2-T02 | Extract entities and value objects | Done 2026-08-04, **three commits so drift would be attributable**. (1) The six dataclasses moved `src/types.py` → `nanoscope/core/entities/`; `src/types.py` is now a shim that defines nothing. Proven mechanically before the gate: identical fields/order/defaults against the pre-move module, and `src.types.X is nanoscope.core.entities.X` — **one `Detection` class, not two**, or `isinstance` lies across the boundary. (2) The strict `nanoscope.*` override caught what legacy code arriving verbatim does not satisfy: bare generics tightened to `dict[str, Any]`; `Detection.bbox` given a scoped `type: ignore` because mypy complaining there **is** D-16, and fixing it moves a number the golden records — `warn_unused_ignores` makes that ignore expire itself when M3 lands. nanoscope back to **0 errors**. (3) `Modality`, `Polarity`, `PixelScale`, `DeviceKind` + 8 mutation-validated tests, **defined but adopted by nothing** — adoption changes `asdict` output, so it belongs to M2-T10 / M3-T10 / M2-T03…T07 / M4-T12. **M2-T13 must not delete them.** `StrEnum` over `(str, Enum)` — ruff UP042 pointed at the stdlib. Golden: zero drift | [x] |
| M2-T03 | Move preprocessing | Done 2026-08-04: → `core/science/preprocessing/`, split into `flatten.py` and `substrate.py`; `src/preprocess.py` is a shim. **Proved by AST comparison before the gate ran** — all six functions code-identical, docstrings differing only in trailing whitespace, and the 5 mypy errors moved with the code (21 before, 21 after). Golden: zero drift. The task that mattered was the collision this exposed: verbatim legacy cannot satisfy a strict package, so the transit status is declared **once in configuration** — mypy at default strictness for `nanoscope.core.science.*`, ruff blocking there but ignoring six named rules (Russian text → M2-T12, `print` → M2-T11, implicit-optional → M3, RET504 cosmetic). Nothing silenced, every entry carries the task that deletes it | [x] |
| M2-T04 | Move I/O parsing | Done 2026-08-04: SPM decoding → `core/science/io/nanoscope_spm.py`; `load_afm`, `load_microscopy_image`, `make_synthetic_afm` → `infrastructure/storage/loaders.py`, because each takes a path and opens it. `src/afm_io.py` is a shim. **The `ImageLoader` port was deliberately not written here** — a Protocol with one implementation and no second caller is what M2-T08 defines wholesale, and writing it twice is writing it twice. Three of ruff's safe fixes landed in `loaders.py` (RET505, UP037, PIE790) and are named rather than hidden behind a claim of verbatim | [x] |
| M2-T05 | Move the LoG detector | Done 2026-08-04: `log_detector.py` and `base.py` → `core/science/detection/`; both shims now. **All 7 definitions AST-identical** — `detect_particles` is the most golden-covered function in the project, recorded for all 8 phantoms at `rtol=1e-6`, and it did not move a number. `yolo_detector.py` stays in `src/` on purpose: it imports torch, so it is infrastructure and belongs to M2-T07 | [x] |
| M2-T06 | Move and split measurement | Done 2026-08-04: → `core/science/measurement/`, split `height.py` (needs a Z map — AFM) from `geometry.py` (needs only a binary mask — any modality). All 5 definitions AST-identical. **This is the split that fixes the D-issue**: mask geometry was trapped in an AFM-named module, which is why `src/segmentation.py` reaches into `src.measure` for SEM/TEM shape metrics; that dependency is now on a modality-neutral module | [x] |
| M2-T07 | Move model-backed code to infrastructure | Done 2026-08-04: `YoloDetector` → `infrastructure/models/yolo.py`, SAM2 runners → `models/sam2.py`, and `afm_to_rgb`/`overlay_masks` → `infrastructure/imaging/colormap.py` (neither has anything to do with SAM2 — same accident M2-T06 untangled). **After this commit nothing under `core` imports torch, ultralytics, sam2 or patched_yolo_infer**, which is what makes the dependency rule true rather than aspirational. AST-verified 4/6 identical, the other two named. `F821` caught two dangling `afm_to_rgb` references before any test ran — the argument for keeping ruff blocking on moved code. mypy 21 → 21, after both modules joined `core.science` at default strictness (strict produced 16 errors in one commit); `infrastructure.storage.loaders` deliberately not exempted, since it passes strict as written | [x] |
| M2-T08 | Define the ports | Done 2026-08-04, **but only `Detector`** — a deliberate narrowing, not an omission. The other six have no implementation, no caller and no second candidate implementation in the repository; an interface written before its first adapter is a guess that gets rewritten once real code has to fit through it, by which point it is quoted in a document and looks decided. `core/ports/__init__.py` carries a table of which task brings each one (LogSink → M2-T11, DeviceProvider → M4-T12, ProjectRepository → M6, TrainingProvider → M7, …); that table is the commitment. `Detector` is a `Protocol` satisfied **today** by `LogDetector` (core) and `YoloDetector` (infrastructure) from opposite layers, neither importing `core.ports`. It does not replace `BaseDetector`, which is inherited and carries `_blobs_to_detections`. 3 tests, the strongest of which is mypy checking the signature structurally; one asserts `import nanoscope.core.science` leaves torch out of `sys.modules` | [x] |
| M2-T09 | Break the import cycles + add the import-graph test | Done 2026-08-04. All five cycles had **one cause** — `src/__init__.py` re-exported the pipeline and detectors, and Python runs a package `__init__` before any submodule, so importing the "dependency root" pulled in SAM2 and matplotlib. Nothing ever used `from src import X`; emptying one file broke all five at no cost to any caller. `import src.types` **1198 → 187 modules, 0.77 s → 0.07 s**; `nanoscope.core.entities` **626 → 185**, matplotlib and pandas gone (the latter moved behind `TYPE_CHECKING` — `from __future__ import annotations` never evaluates it). `tests/unit/test_import_graph.py`: direction checked **statically over the AST** (catches an edge in a module no test runs), weight checked **dynamically in a subprocess**. Both proven to fail; includes a guard against the glob matching nothing. **The M2 exit criterion's "< 100 modules" was unachievable and is corrected to 250** — numpy alone is 141 | [x] |
| M2-T10 | One owned capability matrix, validated before inference | Done 2026-08-04: `nanoscope/application/capabilities.py` is the one executable copy; `PROJECT_CONTEXT.md`'s table now documents it rather than restating it (the third copy, `ConfigPanel.tsx`, was deleted by ADR-0012 — it was the one that had drifted, D-19). **D-14 fixed**: `validate_request` runs before a detector is constructed, so AFM + YOLO + baseline no longer burns a full inference pass before raising. Every rejection message is byte-identical to the old one, in the same most-specific-first order. 12 tests carry the change alone — the golden never calls `run_pipeline` — and the key ones monkeypatch both detectors to explode on construction; commenting out the call turns 2 red | [x] |
| M2-T11 | Structured logging | Done 2026-08-04: 13 `print` calls → `logging.getLogger(__name__)`, lazy `%`-formatting, no library-side configuration. **No `LogSink` port — `ADR-0013`**: it would only wrap `logging`, whose `Handler` is already the extension point, `LogRecord` already the structured payload, and whose handlers are already attached by the application. The SQLite destination becomes a `Handler` in M6. `ADR-0001`'s port list amended. 41 tests: a per-module AST sweep plus real call paths through `caplog`, including silence-by-default. **The golden caught a bug here** — `"1%%"` is only an escape when `logging` formats, which it does not without args | [x] |
| M2-T12 | English-only library code | Done 2026-08-04: 197 lines across **six** modules (`visualization.py` translated in place though it has not moved). `grep -rn "[а-яА-ЯёЁ]"` over `nanoscope/ src/ tests/` returns nothing. **First declared golden change in the project**, and the diff is the argument for having one: **6 lines, not one a number** — 4 translated exception messages plus `stdout_lines` 8→0 and 4→0, the latter being M2-T11 arriving. Re-baselined with `--write`; re-compare clean | [x] |
| M2-T13 | Retire dead code | Done 2026-08-04: **4 deleted, not 10** — `run_full_pipeline`, `plot_pipeline_result`, `plot_detections_histogram`, `make_synthetic_afm`. **Reporting four is the finding.** The audit counted callers; six of its ten are load-bearing for reasons a caller count cannot see: `load_microscopy_image` is the only SEM/TEM entry point and has 4 tests; **`estimate_log_threshold` is recorded by the golden on every phantom** and is the baseline the adaptive variant was adopted against; `run_preprocessing` is the documented entry point M4 wires up; three more are used by the notebooks M1-T09 kept | [x] |
| M2-T14 | Package installation | Done 2026-08-04: `[build-system]` hatchling, `packages = ["nanoscope"]`; `src/` deliberately unpackaged (shipping it would publish `import src`, the collision ADR-0011 renamed away from). Wheel verified: `py.typed` in, no `src/`, 37 modules. **The `pythonpath` hack is half gone**: the `"src"` entry — the real one, which shadowed stdlib `types` and `pipeline` — is deleted; `"."` stays until M2-T15 removes the shims, and the reason is now written down. Also established: **CI does not install the project** (`--only-group ci` installs the group only, deliberately, or torch would return), so CI resolves `nanoscope` through that same entry | [x] |
| M2-T15 | Delete the `src/` shim | Done 2026-08-04: **`src/` deleted entirely.** The title understated it — three modules had never had a shim (`pipeline`, `preprocessing_pipeline`, `visualization`) and had to move first: the two orchestrations to `application/use_cases/`, plotting to `infrastructure/imaging/`. Callers rewired: the golden harness (7 sites), three test modules, both notebooks. **`pythonpath` deleted outright**, mypy now points at one package. A test caught a naming trap — a module and a function with the same name shadow each other through `__init__`, so `use_cases/run_pipeline.py` became `pipeline.py` | [x] |
| M2-T16 | Refresh `PROJECT_CONTEXT.md` to the new layout | Done 2026-08-04: it had drifted past usefulness — describing `src/`, the ADR-0012 frontend, a `pytest.ini` deleted in M1-T05 and a batch script broken since `AFMRawData` landed. Repository map, layer diagram, dependency direction, every path in §5–§10, dependencies, gates, gaps and agent guidance rewritten. The dependency rule is now described as **a test** with the file enforcing it and the measured import weight; the gaps section names the audit ID and M3 task for every defect M2 deliberately did not fix | [x] |

---

## M3 — Numerical correctness ✅ (closed 2026-08-09)

> **Gate for every task in this milestone:** its own commit, its own ADR, its own golden
> update, and a quantified before/after delta in `Progress.md`. Never bundled.

| ID | Task | Defect | Severity | Status |
|---|---|---|---|---|
| M3-T01 | Fix `build_substrate_map(manual_radius_px=...)` — `UnboundLocalError` on 100% of calls | D-01 | critical | [x] Done 2026-08-04, **ADR-0014**. One line: `opening_radius = manual_radius_px`, the value actually passed to the opening. **No rounding, no floor** — both would pre-empt B4/M3-T09 and M3-T13. Delta: **50 golden differences, all under `build_substrate_map_manual`** (10 fields × 5 phantoms); the automatic path 100% of real callers use is untouched. The harness was extended in the same commit to record the returned arrays, or fixing the defect would have left the branch *less* characterized than while broken. 6 tests; restoring the bug turns 5 red |
| M3-T02 | Fix `min_size_pixel` flooring to zero on 90% of real scans | D-04 | critical | [x] Done 2026-08-06, **ADR-0024**, decision **B2 → filter in nanometres, delete the `int()`**. `estimate_radius_otsu` and `estimate_rough_radius` take `min_size_nm`; the comparison is `radii_nm >= min_size_nm` and the three conversion sites in `build_substrate_map` are gone. Delta: **47 golden differences — 27 changed, 15 added, 5 removed**; **`afm_sparse_low_snr` 75 → 17 objects** and everything derived from its radii, the other four AFM phantoms byte-identical, **no measured height moves anywhere** (the final opening radius is 8 on both sides). Re-reading all **628** scan headers reproduces the audit's 90% and adds the part it did not measure: the zero threshold cost nothing on the **365 (58%)** scans coarser than 8.86 nm/px, where one pixel is already over 5 nm; it cost the noise filter on the **203 (32%)** in the 5–8.86 band; and the finest **60 (10%)** were hurt by truncation rather than by the floor, which is `afm_sparse_low_snr`'s case (2.5 px → 2 px). 5 tests; restoring the `int()` turns 3 red. The duplicated `radii_nm` assignment went with it — the change forced the line to move |
| M3-T03 | Fix YOLO input: normalise **then** cast (12.6% of dynamic range currently survives) | D-03 | critical | [x] Done 2026-08-05, **ADR-0015**. The cast moved after the normalisation; `min_size`-style semantics untouched. Delta: **67 golden differences, all under `yolo_input_preparation`**, on all 7 phantoms. Grey levels reaching the network: 8–208 → 239–256. The harness's D-03 measuring stick, `mean_abs_diff_vs_normalize_first`, now reads **0.0** everywhere and becomes a permanent guard. 6 tests; restoring the order turns 5 red. **Not claimed: better detections** — the weights were trained on images the old path produced (see the ADR) |
| M3-T04 | Aspect-ratio-preserving YOLO letterbox; isotropic box rescale | D-21 | medium | [x] Done 2026-08-05, **ADR-0016**. Isotropic scale to fit, pad to the square with 255 (substrate after inversion, applied *after* the normalisation), and one shared geometry helper so the forward and inverse maps cannot drift. Delta: **0 golden differences, 7 keys added** — a square scan is byte-identical, and every phantom is square, which is why the harness gained `non_square_half_height`. 5 geometry tests; restoring the squash turns 4 red. Found while reading: **M3-T21** |
| M3-T05 | Propagate YOLO confidence into `Detection` | D-09 | medium | [x] Done 2026-08-06, **ADR-0028**. The model scores every box, `cfg.yolo_conf` filters on those scores, and the conversion dropped them — so every YOLO detection reported **1.0**, including one that had just cleared the threshold. Both backends now pass their own scores (`boxes.conf`; `CombineDetections.filtered_confidences`, post-NMS), and a length mismatch **raises** rather than being `zip`ped away, because a score attached to the wrong box reads as a measurement of that box. **`confidence` is now `float \| None`, defaulting to `None`:** `1.0` was a substitute value, the fifth this milestone has deleted, and it made the LoG detector claim certainty it never computed — its blob response is not a probability, and normalising one would be a claim only **M3-T15** could license. Delta: **29 keys added, 0 values changed**. The finding is that `contracts.default_detection_confidence` is `ADDED`: the harness recorded the defaults of D-16's field and not of D-09's, one line below, **so the golden could never have caught this**. 7 tests; restoring the drop turns 6 red. mypy **14 → 12** — the second array would have added a third `_last_result` error, and annotating that field removed all three |
| M3-T06 | Otsu sizing: raise on empty-after-filter; report post-filter `n_objects` | D-05, D-06 | high | [x] Done 2026-08-05, **ADR-0017**. The empty-after-filter case raised nothing and returned `nan`; it now raises and names the parameter, its value and the largest object measured. `n_objects` counts survivors. Delta: **8 golden differences** — `n_objects_reported` **1023 -> 75** on `afm_sparse_low_snr` (13.6x over-count), the `extreme_aspect` degenerate input now fails as `estimate_radius_otsu` instead of as `cannot convert float NaN to integer` in `build_substrate_map`, and 5 keys added for D-05's own reproduction. 4 tests; restoring the old behaviour turns 3 red. **It also broke an M3-T01 test that had been passing on the `nan`** |
| M3-T07 | Guard LoG normalisation against a zero maximum | D-11 | medium | [x] Done 2026-08-05, **ADR-0018**. Both `z_above / z_above.max()` sites now stop on a non-positive or `nan` maximum: the threshold estimator returns the named `DEFAULT_THRESHOLD = 0.05`, `detect_particles` returns an empty `(0, 4)` — zero particles is an answer, not an error (the opposite call from ADR-0017, and the ADR says why). Delta: **65 golden keys added, 0 changed** — the working path is byte-identical, and the number that was wrong (an adaptive threshold of **2.4997** on a negative map, outside the `[0, 1]` it is compared against) had **never been recorded**. Two harness fixes in the same commit made it visible: a `negative_with_structure` degenerate input, and scalars recorded instead of the string `"non-array"`. 11 tests; restoring the raw division turns 3 red |
| M3-T08 | `flatten_lines` must promote dtype like `flatten_plane` does | D-13 | medium | [x] Done 2026-08-07, **ADR-0029**. `np.empty_like(z)` kept the input's dtype, so the float64 residuals `polyfit`/`polyval` produce were cast back on assignment. The allocation now promotes with `np.promote_types(z.dtype, np.float64)` — `flatten_plane`'s own rule rather than a float64 cast that agrees with it by coincidence. Delta: **13 golden differences — 8 dtypes `float32 -> float64`, 4 sums, 1 added group; no phantom moves**, because every recorded chain is float64 by the time `flatten_lines` sees it, which is why the golden never caught this. **The audit understated it:** it measured a ramp with sub-1 residuals and reported "all zeros", but on an image with real structure an integer output does not truncate a negative residual, it **wraps** — on the newly recorded 8-bit phantom the levelled map is wrong by up to **257**, every pit rendered as a peak. Boolean input, which the audit did not measure, came back as a *mask* of where the residual was non-zero. The four moved sums are the fix as a physical property: a least-squares residual sums to zero, and float32 storage was leaving it at **1e-6** instead of 1e-13. 17 tests; restoring `np.empty_like(z)` turns **14** red, the three survivors being the float64 cases. mypy unchanged at 12 — a dtype that is right for one input and wrong for another has no static shadow |
| M3-T09 | Define and apply the opening-radius rounding rule (half-integer radii break `disk()` centring) | D-10 | medium | [x] Done 2026-08-05, **ADR-0020**, decision **B4 → round up**. Every radius reaching `disk()` is `ceil`-ed, in `get_substrate_map` — the funnel all three sites pass through — and `build_substrate_map` reports the integer it used, keeping ADR-0014's principle intact. Up, not down: a radius smaller than a particle recovers a "substrate" containing the particle. Delta: **696 golden values move, 0 keys added** — opening radius +1 or +2 on all five AFM phantoms, **no particle count changes**, largest height move **0.049 nm (0.37 %)** on `afm_dense_overlapping`. mypy **18 → 15**, and the three errors that went were this defect's static shadow. 11 tests; restoring the floor turns 4 red |
| M3-T10 | Detection polarity: TEM currently returns 0 of 22 particles | D-12 | high | [x] Done 2026-08-05, **ADR-0023**, decision **B3 → configured, per-modality default**. `Polarity` (written in M2-T02, adopted by nothing until now) is a `PipelineConfig` field whose `None` resolves to the modality's convention; both detectors take one. One inversion at the entrance (`max - z`, its own inverse and positive-maximum-safe per ADR-0018) and, on the YOLO side, `_prepare_image` inverts only a bright-on-dark image. Delta: **`tem_dark_particles` 0 → 22 of 22 blobs**, prepared YOLO input mean grey 43.3 → 211.7, `config_fields` 12 → 13; **SEM and all five AFM phantoms byte-identical**. Not claimed: better YOLO detections — inference is outside the gate. 14 tests |
| M3-T11 | Handle unknown pixel scale (`None`) without crashing both detectors | D-07 | high | [x] Done 2026-08-05, **ADR-0019**. `None` now propagates and the nanometre value becomes absent: `Detection.radius_nm` is `float \| None`, the LoG blob array's nm column is NaN (an ndarray cannot hold `None`) and `_blobs_to_detections` maps it to `None` at the entity boundary. Pixel-space output is bit-identical with and without a scale. Delta: **168 golden keys added, 0 changed** — every phantom has a scale, so nothing recorded moves; the new keys record a path that used to raise `TypeError`. **mypy 19 → 18**: the error it removes, `pipeline.py:62`, *was* D-07 reported at the assignment instead of at the crash, and had sat in the baseline since M1-T04. 8 tests; substituting the tempting wrong fix `pixel_size_nm or 1.0` turns 4 red |
| M3-T12 | Empty measurements must return a schema-stable DataFrame | D-08 | high | [x] Done 2026-08-06, **ADR-0027**. `pd.DataFrame([])` has **zero columns**, so a scan with nothing measurable answered every read by name with `KeyError`. The baseline schema is now declared — twelve columns, each with a dtype — and returned whether or not a row survived; a test proves the declaration still describes what the **populated** path emits, which is the only thing keeping a declared schema honest. Delta: **78 golden differences, all columns appearing where there were none, 0 values moved**. The sixth block is the finding: `afm_sparse_low_snr` detects **0 blobs on its ordinary path**, so D-08 was live on a real phantom's normal run and the golden had been recording `columns: []` for it since the baseline. 7 tests; restoring `pd.DataFrame(results)` turns 3 red. **Out of scope, and why:** the two SAM2 producers vary their columns *per row* (D-16/D-17 → M3-T14), and `run_pipeline`'s detect-mode empty frame needs the modality-dependent schema T14 decides. **Found while testing:** a NaN height passes `<= 0` and reaches the table — filed as **B-059**, not fixed here (ADR-0010) |
| M3-T13 | Typed error taxonomy + input validation at every numerical entry point | D-15 | medium | [x] Done 2026-08-07, **ADR-0030**. Seven classes in `core/errors.py`, each also inheriting **the builtin it replaced at its site**, so every existing `except ValueError` in the notebooks keeps catching what it caught; and one `ensure_height_map` in `core/validation.py`, called at **fourteen** entry points. A height map is 2-D, non-empty, of an integer or real dtype, and **finite** — the last being the decision, since `flatten_plane` already enforced it through `scipy.lstsq` while `flatten_lines` propagated NaN and `detect_particles` answered a NaN map with "no particles". Delta: **129 golden differences and not one measured value** — 32 exception types, 28 messages that became ours, 15 `raised_in`, and **13 cells that used to answer an input they could not use**. `TypeError`, `IndexError`, `LinAlgError` and `RuntimeError` each collapse into one of ours; **foreign messages in the golden 15 → 0**, emptying the `_unchecked` category ADR-0022 created. The twelve phantom-level differences are exception types on two probes that were already failing. **Supersedes ADR-0018 on non-finite input only** (a flat or negative map still answers "no particles") and M3-T08 on boolean input, which is now refused rather than corrected. 109 tests, the centre being 7 bad inputs × 10 entry points = **70 combinations, one error type**, plus the same sweep proving a valid map passes all ten. **B-060** (masked levelling) and **B-061** (a rough radius of 0) filed rather than smuggled in. mypy unchanged at 12 |
| M3-T14 | One measurement schema across all four producers; fix the `bbox` contract | D-16, D-17 | medium | [x] Done 2026-08-07, **ADR-0031**. `core/science/measurement/schema.py` declares a **core** every producer emits plus **blocks that are present in full or absent in full** — detector, height, geometry, segmentation — and `method` names the producer, so a reader knows which blocks to expect. Not one wide table with NaN: SEM/TEM does not have heights that are all missing, it has no heights. Reading the four producers found **three** faults where the audit named one: `score`/`sam_score` and `mask_area_px`/`area_px` are one quantity under two names, both SAM2 producers built rows with `if k in res` so the columns varied **per row**, and — the one the audit missed, and the worst — **`radius_nm` was two quantities under one name**, the detector's blob radius in the baseline table and the measured mask's radius in the SEM/TEM one, so concatenating them mixed two measurements in one column. Now `detector_radius_nm` (where we looked) and `radius_nm` (what we found). **D-16**: `bbox` is `tuple[...] \| None`, defaulting to `None`; the M2-T02 `type: ignore` written to expire itself expired. Detect mode returns the modality's empty table — the case ADR-0027 left open by name. Delta: **62 differences, all names, dtypes or added columns; 35 column digests unchanged, 0 changed**, and the renamed column's digest is byte-identical to the one it replaced. The harness needed the same fix the code did — `list(det.bbox)` is a `TypeError` once a bbox can be absent, D-16's assumption living inside the tool meant to catch it. 31 tests over five tables, the SAM2 pair driven by a **stub predictor** because there are no weights here or in CI, which is also why their golden delta is zero **by construction** and the tests are the whole safety net. mypy unchanged at 12 — two new errors appeared and were fixed, one of them a dead comparison mypy caught in code written five minutes earlier |
| M3-T15 | Evaluation harness: precision / recall / localisation vs phantom ground truth | — | — | [x] Done 2026-08-07, **ADR-0032**. `core/science/evaluation.py` — not `tests/`, because M4's annotation flow and M8's training loop need it. A match is a centre within `match_factor x the particle's own radius` (scale-free; the phantom set spans 1.95–29.3 nm/px, so a fixed pixel threshold is two different physical tolerances), and the assignment is **one-to-one and optimal** via `linear_sum_assignment` — ten boxes on one particle are 1 TP and 9 FP, and greedy nearest-first can pair the wrong two and mismeasure the localisation error. Ratios with a zero denominator are `None`, not a substituted 1.0. Delta: **7 golden differences, all `detection_quality: ADDED`** — nothing moved. **The first quantitative statement about detection quality in this project:** four AFM phantoms and both image phantoms score precision and recall **1.000** with a localisation error of 0.36–0.61 px; `afm_dense_overlapping` scores **P 0.983 / R 0.843** (11 misses of 70 where particles overlap); and **`tem_dark_particles` is 22 of 22 at 0.36 px**, which turns ADR-0023's "0 → 22 blobs" from a count into a measurement. Found and filed, not fixed: **B-062** — `afm_sparse_low_snr` scores **recall 0.000**, six known particles and none found. 21 tests, including a constructed case where greedy costs 6.0 against the optimum's 4.0. **Not licensed, and said before the numbers existed: a phantom is not a sample** |
| M3-T16 | Characterize `_read_nanoscope_z` against multiple Nanoscope versions | gap | — | [ ] |
| M3-T17 | `_read_nanoscope_z` divides `None` by `samps` when the header has no `Scan Size` — the fallback branch crashes on the next line (`afm_io.py:95-98`) | **new**, found by mypy in M1-T04 | high | [x] Done 2026-08-06, **ADR-0026**. **D-07 is now closed on all three faces** — detectors (M3-T11), the npy loader (M3-T20), the SPM header (this). A header with no `Scan Size` returns `(None, None, z)`: the height map decodes as always and only the metadata is absent. The same expression's other failure mode went with it — `Samps/line: 0` was a `ZeroDivisionError` naming nothing — and a *stated* non-positive `Scan Size` is now rejected too, which is ADR-0025's absent-versus-wrong distinction applied to the second loader. The annotation stops lying (`-> tuple[float \| None, float \| None, np.ndarray]`): **mypy 15 → 14, and the error that went was this defect's**. Delta: **0 golden differences, and none was possible** — `afm_io` has no phantom, so its 28 unit tests are the whole safety net. 3 tests; restoring the division turns 3 red |
| M3-T18 | `YoloDetector._last_result` is initialised to `None`, so its type is `None`; `.filtered_boxes` is accessed unguarded (`yolo_detector.py:50,87,99`) | **new**, mypy | medium | [x] Done 2026-08-06 **as a side effect of M3-T05**, not as its own commit. Threading the confidences through `_detect_tiled` would have added a *third* `"None" has no attribute ...` error on this field, so it was annotated `Any` — which its own comment already described — and all three went. mypy **14 → 12**. **No runtime guard was added, and none is needed:** every access sits two lines below the assignment inside the same method. The public `last_result` property can still return `None` before the first `detect()`, which is the documented meaning of the field, not a defect. `Any` rather than a real type because both possible classes live in optional heavy dependencies that must not be imported at module level |
| M3-T19 | `estimate_log_threshold_adaptive` rebinds `responses` from `list[float]` to ndarray before calling `.min()`/`.max()` (`log_detector.py:111,116`) | **new**, mypy | low | [x] Done 2026-08-09, **no ADR** (the M3-T18 precedent — an annotation is not executed and no decision is made). Reading the file for the rebinding turned up the same fault stated the other way round, twice: `detect_particles(threshold: float = None)` and `build_substrate_map(manual_radius_px: float = None)` — **implicit Optional**, where the annotation promises a number the body immediately tests for `None` (`log.py:237`, `substrate.py:334`). One defect class — *an annotation that does not describe the value the code carries* — and **6 of mypy's 12 errors**. Delta: **zero, and not by measurement — no executable line changed**; golden byte-identical, 478 tests. mypy **12 → 6**, and what remains is not annotation drift: four in `pipeline.py` (three `ndarray \| None` arguments and the `if/elif` detector dispatch, both M4's), two third-party overloads. 2 tests: an **explicit** `None` equals the omitted argument at both entry points, which is the meaning `float \| None` now claims. **Recorded, not filed:** `r = max(int(sigma), 1)` at `log.py:165` is a truncation of the family M3-T24 hunted, but it sizes a peak-lookup window, not a physical radius, and no defect can be demonstrated from one pixel there |
| M3-T20 | `load_afm(fmt="npy")` fabricates a physical scale: `pixel_size_nm or 1.0` and `scan_size_nm or float(z.shape[0])` (`afm_io.py:132-133`). Unknown scale must be `None` — the invariant D-07 states. Two consequences: every downstream `_nm` becomes a pixel count wearing nanometre units, and `or` also swallows an explicit `0.0`. Row count is used as a length in nm, which is not even dimensionally a size | **new**, found by the M1-T06 tests | high | [x] Done 2026-08-06, **ADR-0025**. **D-07 is now closed on both sides.** The loader passes through what it was given; `AFMRawData` and `PreprocessingResult` carry `float \| None`; a scale that *is* given must be positive, so `0.0`, `-1` and `nan` raise instead of being swallowed by `or`. `build_substrate_map` accepts `None`: the `_nm` outputs are `None` and the `min_size_nm` filter cannot be applied, which is **warned**, never silent (silent is D-04). Delta: **5 golden keys added, 0 values changed** — every phantom has a scale. What the new keys record is the finding: an unscaled run equals a scaled one with `min_size_nm=0`, so on the four clean phantoms the substrate is **bit-identical**, and on `afm_sparse_low_snr` it is **not** — 17 objects become **3351**, the typical radius falls 2.99 px → 0.80 and the opening radius 8 → 5. Losing the scale means losing the filter, and where the filter was load-bearing that reaches the substrate. 10 tests; restoring `pixel_size_nm or 1.0` turns 6 red |
| M3-T21 | `use_tiling=True` — the default backend — produces **exactly one crop**. `_prepare_image` hands `MakeCropsDetectThem` a 640x640 image and the crop shape is also 640x640, so `get_crops_xy` computes `int((640-640) / (640*0.75)) + 1 = 1` step on each axis. The sliding window covers the whole image in a single tile: the tiled backend does the same work as the direct one, more slowly, and small particles are never seen at native resolution — which is the only reason tiling exists. Real tiling needs an input of at least `shape * (2 - overlap/100)` = 1120 px, and a 512x512 scan cannot reach it. Fix is a decision between a smaller crop shape and feeding the scan at native resolution (which then needs D-21's letterbox to survive the library's own resize) | **new**, found while reading for M3-T04 | high | [x] Done 2026-08-05, **ADR-0021**, decision **B7 → keep the backend, stop defaulting to it**. `use_tiling` now defaults to `False` in both `YoloDetector` and `PipelineConfig`; the degenerate case logs what it will do; `_crop_steps` turns the arithmetic into a tested method. Delta: **zero golden difference**, because inference is outside the gate — but the backends are *not* bit-identical even at one crop (`CombineDetections` runs a second NMS), so real detections may move and **no claim is made that either is better**; M3-T15 owns that. Not deleted: the fix is an input-size trade-off nothing can measure until M3-T15 exists. 9 tests |
| M3-T22 | A NaN height passes the non-positive filter and reaches the measurement table (`height.py`) | **B-059**, found while writing M3-T12's tests | medium | [x] Done 2026-08-07, **ADR-0033**. `if metrics["height_nm"] <= 0` was written to discard artefacts, and **`nan <= 0` is `False`**, so the most artefactual value there is was the one it let through. The route: a constant map has no Otsu split, so the substrate mask is **empty**, `np.median` of nothing is `nan`, and every particle that falls back to the global baseline inherits it — two rows of `NaN` in a table of measurements, with nothing said. The comparison needed no new decision (**ADR-0018** ruled on `not x > 0` for this exact reason); what this task decided is that an **empty substrate mask stops being silent**, because the fix alone turns two `NaN` rows into zero rows, which reads like "there was nothing here". Delta: **5 golden differences, all the new probe** — the fix moves nothing recorded, because **no phantom has an empty substrate**, which is why the golden could not catch it; fifth time in M3 that closing a defect meant extending the harness that missed it. **Found while testing:** the empty substrate is **all-or-nothing** — `get_clean_ring` intersects the ring with the substrate mask, so no particle can be measured at all and there is no partial case. 10 tests; restoring `<= 0` turns 2 red. mypy unchanged at 12 |
| M3-T23 | A rough opening radius of 0 makes the opening the identity, so the "substrate" is the image | **B-061**, filed by M3-T13 | medium | [x] Done 2026-08-07, **ADR-0034**. `estimate_rough_radius` could return **0**; `disk(0)` is a single pixel, so the rough opening is the **identity** — the substrate comes back equal to the image and `z_above` is zero everywhere, with the shape of an answer. The condition is that `median + std` selected single-pixel noise: the median object area is **1.0 px** on `afm_sparse_low_snr` in *both* the scaled and unscaled runs, and the scaled one survives only because `min_size_px = 5/1.95 = 2.56` floors it — **the estimate is equally worthless there and the floor hides it**. A sub-pixel estimate now takes the fallback the function already had for "too flat or too noisy" (1 % of the image width), checked **before** `_integer_radius` because `ceil(0.96)` is 1; it lands on **3 px**, which is what the scaled run of the same image computes. **It corrects ADR-0025's diagnosis:** that ADR recorded 17 → 3351 objects and called it "losing the scale is losing the filter" — losing the scale did *two* things, and this was the bigger one: **3351 → 627**. Delta: **11 differences, all in one cell**; `opening_radius`, `substrate` and `z_above` are **unchanged**, because the median radius that drives the final radius is the same. **The golden had the evidence all along** — an Otsu threshold of **7.7e-09**, which is Otsu on the all-zero map an identity opening produces — and nobody read it. 9 tests; removing the branch turns 5 red. Filed with measurements, not fixed: **B-063**, the `int()` truncation that is the proximate cause and moves every phantom |
| M3-T24 | `estimate_rough_radius` truncates its own estimate with `int()` before rounding it | **B-063**, filed by M3-T23 | medium | [x] Done 2026-08-08, **ADR-0035**. Two roundings in three lines, in opposite directions, only one documented. **The rule was already decided three times over:** ADR-0020 (`_integer_radius` is the one funnel, and radii round *up*), ADR-0024 (deleted this exact `int()` as D-04's mechanism), and the parameter's own docstring from March 2026 — `scale=1.7` is *"a multiplier so the disk is safely **larger** than a particle"*, and the truncation makes it smaller. So this applies a rule rather than making one, which is why it is not an operator decision. Delta: **730 differences across four phantoms** — large in count, small in magnitude: 320 under 1 %, mean measured height moves **≤ 0.09 %**, and the largest scientifically meaningful move is 2.5 % at one phantom's 90th height percentile. **The task's own plan predicted ~70× less, and the correction is the finding:** the two-stage design absorbs the rough radius for the *final* radius (which moves on 1 of 5 phantoms), but `sizes` also feeds `estimate_log_params`, so the LoG **sigma range** shifts wherever the rough radius did — 379 of the 730 differences are `log_detection`. **`afm_dense_overlapping` detects one more particle with a byte-identical substrate**, which is the clean demonstration. Detection quality, checkable for the first time (M3-T15): **recall unchanged on every phantom**; on the one phantom whose substrate moved, radius error improves 6 % and localisation degrades 0.3 % in the mean — **a wash, reported as one**. 18 tests; restoring `int()` turns 11 red. **B-064** filed: the provenance of `1.7` and `2.5`, now measurable |
| M3-T25 | Levelling that fits around a dropped scan line instead of refusing the scan | **B-060**, filed by M3-T13 | medium | [x] Done 2026-08-08, **ADR-0036**. M3-T13 made a non-finite value a rejection — right as a uniform contract, and ADR-0030 said in its own text it was not the best behaviour available. **A dropped scan line is a real artefact:** two rows of NaN and four thousand good ones, and the whole scan was refused. `flatten_plane` and `flatten_lines` now take **`allow_gaps=False`** and fit over the finite pixels when asked. **Opt-in, because accepting NaN silently would put the library back where D-15 found it** — levelling tolerating what detection refuses. The gap stays **absent**, never interpolated (an interpolated value is a measurement nobody made — the eighth substitute value this milestone declined to add), and a row with too few finite points comes back absent with the count **warned**. Delta: **5 differences, all the new `gapped_levelling` block; nothing recorded moves**, exactly as the plan predicted. The block carries the evidence, not just the result — masked fit, ungapped reference and the `nan_to_num` alternative per phantom — and the finding is that **the advantage tracks the tilt**: 4.2× on `afm_tilted_polydisperse`, 1.2–1.7× on the flat ones, because zero-filling corrupts the *plane* rather than adding noise. **Honest headline: this does not make the pipeline gap-tolerant** — the output still carries NaN and the substrate step still refuses it, pinned by a test. **B-065** (gap-tolerant substrate/detection — needs a decision about what a substrate under a gap is) and **B-066** (deliberate interpolation) filed |
| M3-T26 | The opening-radius constants are undocumented and were chosen against a truncation | **B-064**, filed by M3-T24 | medium | [x] Done 2026-08-08, **ADR-0037**. `scale=1.7` and a bare `2.5` literal set every opening radius in the project; neither was derived anywhere, and both were chosen while ADR-0035's `int()` truncation was in place — the effective margin was `1.7 × int(r)/r`, **1.39 at r = 4.9**. Both swept over the five AFM phantoms against ground truth, which M3-T15 made possible. **The rough factor barely matters:** recall and precision are *identical* from 1.3 to 2.4, because the second stage re-estimates from Otsu — M3-T24's finding from the other side, and **why the truncation survived five months: a constant whose value does not matter does not get audited**. **The final factor is a real trade-off:** smaller finds more particles (dense recall 0.886 at ×1.5 → 0.800 at ×4.0, as a bigger disk steps *over* two touching particles instead of into the gap), larger measures radii better; ×2.5 minimises the radius error on both hard phantoms and is the only value in the sweep not beaten on the metric it leads. **Decision: keep both, name them, expose the literal** — a magic number inside a branch is not a decision anyone can revisit, which is why this took two tasks to surface. `DEFAULT_ROUGH_SCALE`, `DEFAULT_OPENING_SCALE`, `MIN_OPENING_RADIUS_PX`, and `opening_scale` as a parameter. Delta: **zero, golden byte-identical**. `afm_sparse_low_snr` scores 0.000 at *every* factor, which is more evidence B-062 is a detector question. **B-067** filed: the margin comes from the *median* particle, and the polydisperse phantom is the only one that loses a detection as the factor grows |

---

## M4 — Application layer ✅ (closed 2026-08-12)

| ID | Task | Status |
|---|---|---|
| M4-T01 | Specify the project directory format (layout, schema version, compatibility rules) | [x] Done 2026-08-09, **ADR-0038**. First because everything else in M4 writes into a project: left implicit, the format becomes whatever the first task to land happens to do, and unlike every other contract here **the operator's data is on the far side of it**. ADR-0003 fixed the layout and deferred the contract to this task by name. Three decisions: **two independent version numbers** (`format_version` in the manifest, `schema_version` as the database's own `PRAGMA user_version` — a schema bump must not claim the directory changed, and the layout has to be readable *without opening the database*); **`project.json` is the identity file**, not a database row, because ADR-0003's "corruption is contained" is a slogan if a project with a damaged database cannot say what it is; and **refuse newer, accept older** — a forward migration cannot be written by the past. One integer, not semver: one reader, shipped with the writer, asks one question. Ships with the executable half (`infrastructure/storage/project_format.py`) because a spec nothing executes drifts within two tasks. **Found while writing the tests:** the rule that unknown fields survive a rewrite is not documentation — without it an older application rewriting a newer manifest **deletes what it does not recognise**, data loss with no error attached, so the manifest carries them through. A non-integer version is refused rather than coerced (`"2"` is neither newer nor older; `True` **is** an `int` in Python). `ProjectFormatError` joins the ADR-0030 taxonomy — one error for four refusals, each message naming the path or both versions, and a `ValueError` rather than a `FileNotFoundError` because the claim is about a directory that exists. 21 tests over every row of the matrix. Delta: **zero, no numerical code touched** |
| M4-T02 | SQLite schema v1 + a forward-migration mechanism | [x] Done 2026-08-12, **ADR-0039**. The mechanism before the tables, because creating them is its first job — version 0 is an empty file and every table in existence was made by a migration step. `MIGRATIONS` is an ordered list of `(version, statements)` with **`SCHEMA_VERSION` derived from its last entry**, so the constant cannot disagree with the list; a shipped step is never edited. **The finding is a stdlib trap:** Python's `sqlite3` opens a transaction implicitly before **DML only**, so `CREATE TABLE` runs in autocommit and a step failing on its third statement would leave two tables behind *at the old version* — the one state a migration must never produce. Each step now runs under an explicit `BEGIN`, with `PRAGMA user_version` inside it (it lives in the database header and is transactional), and a test breaks a step on purpose to prove nothing survives. **v1 holds one table, `images`**, because a table with no reader is columns designed before their first caller and "no destructive migrations" makes removing a wrong one expensive — M2-T08's rule for ports, one layer down with the operator's data attached. **No WAL:** `-wal` and `-shm` would be two files in a *published* layout, and a project copied mid-write would leave committed data in a file the operator did not know to copy; one desktop writer buys nothing from it. `PRAGMA foreign_keys` is set in the connect helper — SQLite defaults it **off**, per connection, and it is a silent no-op inside a transaction, so a later `REFERENCES` clause would have been decoration. Two rules became `CHECK` clauses rather than conventions: a stored path is relative (ADR-0003's "be careful never to write an absolute path" — care is not a mechanism) and `modality` is one of the three `Modality` members, with the test iterating the enum so the SQL copy cannot drift. A newer `schema_version` raises the same `ProjectFormatError` as a newer `format_version`: one sentence to the operator. 23 tests, delta **zero, golden byte-identical**, mypy unchanged at 6, no new dependency |
| M4-T03 | `ProjectRepository` implementation + integration tests | [x] Done 2026-08-12, **ADR-0040**. **The debt ADR-0003 wrote down and never collected:** *"deleting a file behind the application's back produces a dangling row; the repository layer must reconcile"* — the check had no home until there was a repository to put it in, and it is the piece most likely to be skipped, because everything works in a test where nobody deletes anything. **It reports, in both directions, and changes nothing.** A dangling row is not deleted: a missing file is as likely to be an unmounted drive or a half-finished copy as a deletion, and **the row carries the annotations and measurements the file does not** — the automatic cleanup destroys the more expensive half of the pair, on *open*, unasked. A file no row claims is not imported either, because adopting it means guessing it was meant to be in the project and inventing a modality for it. **Existence, not contents:** verifying checksums would read every scan on every open (628 SPM files in `data/` alone), so the checksum stays in the row for the question that needs it. `add_image` **computes the checksum itself** — one accepted from a caller can describe a different file, and then it only proves two callers agreed — and refuses a file that is not there, since that row is precisely the dangling one. Relative paths are enforced here with M4-T02's `CHECK` as the backstop: the constraint makes the rule true of the *database*, the repository makes the refusal a readable sentence; an absolute path inside the project is converted rather than rejected, because pushing that conversion into every caller is where it goes wrong. Queries return `ImageRecord`, never a `sqlite3.Row`. **The `ProjectRepository` port arrived with its first adapter**, as `core/ports/__init__.py` promised in M2-T08 — load-bearing, not ceremonial: M4-T04's use cases live in `application/`, which may not import `infrastructure`. 30 integration tests in a new `tests/integration/`, including ADR-0003's own compliance clause at last — a project **moved** and a project **copied**, with every path resolving, and `cache/` deleted between two opens. Delta: **zero, golden byte-identical**, mypy unchanged at 6 |
| M4-T04 | Use cases: `CreateProject`, `OpenProject`, `CloseProject`, `ImportImages`, `ListImages` | [x] Done 2026-08-12, **ADR-0041**. **M4's first exit criterion is met** — a project is created, opened, populated and closed from Python, headless, and an integration test runs exactly that sentence. **Two of the five use cases were written, and the other three are the decision.** `open_project` opens *and reads the integrity report*, which is where ADR-0040's closing obligation is discharged; `import_images` carries real policy — a batch of forty scans does not abort on the third, each failure is collected with its path and reason, and the caller gets both halves. `CloseProject` and `ListImages` would be `repo.close()` and `repo.list_images()` under new names: **a function that forwards one call to one object is not a layer, it is a second name for the same method** — and the second name is what later disagrees with the first. They come back when closing or listing means more than closing or listing (M4-T09). `CreateProject` is `SqliteProjectRepository.create` instead, because it is `mkdir`, a manifest and SQLite end to end and `application` may import none of them (Architecture §3.2), with PROJECT_RULES §2.7 already naming the composition root as what constructs adapters. Same judgement as M2-T08's seven-ports-one-written, recorded so "only two exist" reads as a decision in six months. **Only `NanoscopeError` is caught in the batch:** a file the library rejects is data and belongs in the report, anything else is a bug, and a bug that keeps going for another thirty-nine files is worse. A colliding name is **disambiguated** (`scan.spm` → `scan_1.spm`, checked against the *filesystem* so an untracked file is never overwritten) while `display_name` keeps the operator's name; the **same file imported twice is two images**, deferred out loud because deduplication needs a `UNIQUE` index, a migration and an operator's answer to whether two identical scans are ever legitimate. `create` refuses a non-empty directory — a manifest dropped into `Documents/` would make it a project (ADR-0038). 25 tests: 11 against a **second implementation of the port**, which is what stops it being a type alias for one class, and 14 end to end. Delta: **zero, golden byte-identical**, mypy unchanged at 6 |
| M4-T05 | Use cases: `RunDetection`, `RunSegmentation`, `MeasureParticles` | [x] Done 2026-08-12, **ADR-0042**. **M4's second exit criterion is met** — detection and measurement results round-trip through SQLite *and* the filesystem, which is the split the criterion names and this task's main decision: **the index is relational, the tabular product is a file.** `analysis_runs` and `detections` are schema v2; the measurement table is written to `results/run_<id>/measurements.csv` because **ADR-0031 made that table variable by construction** — a relational shape for it is one wide grid with NULLs, which ADR-0031 rejected in those words, or an EAV pivot that loses the declared dtypes. It reconciles the two halves of ADR-0003, whose layout says `results/` holds measurements as files while its storage rule says SQLite holds them. **One use case, not three:** the three names are `run_pipeline` with `mode` set to three strings `PipelineConfig` already carries — ADR-0041's rule, one task later. A `detect` run writes **no** table (an empty one would claim a measurement happened) and a *missing* table raises rather than reading as empty, which would report "no particles" for a run that found some. Results **cascade** with their image — ADR-0040's keep-the-row argument does not transfer to derived data — and the cascade is what finally makes M4-T02's `PRAGMA foreign_keys` load-bearing. Masks are **not** persisted: SAM2 weights are outside the gate, so the format would be written blind; the trigger is named (M5's viewer, or M4-T07). **Found by a test, and it was live:** `run_analysis` was passing an npy to the loader without the scale the project had recorded at import, so an image imported *with* a known scale was analysed as though it had none — every `radius_nm` `None`, the physical minimum-size filter silently skipped. **The D-07 family of defect M3 spent a milestone removing, reintroduced one layer up.** `run_preprocessing` gained a `pixel_size_nm` parameter and a test pins it. **Schema v2 is also the first migration applied to a database with rows in it** — the case ADR-0039's mechanism was built for and had never run. 15 tests over a characterization phantom in a real project. Delta: **zero, golden byte-identical** — the first task in M4 where that claim was not free, since the science is called |
| M4-T06 | Job abstraction: submit, progress, cancel, failure reporting | [x] Done 2026-08-12, **ADR-0043**. Thin over `ThreadPoolExecutor`, which already does submit, results, exceptions and cancelling a job that has not started; **progress and stopping one that has** are the whole module. **A job is a handle, not a base class** — `DetectionJob(Job)` and `ImportJob(Job)` differ only in which function they call, ADR-0041's rule for the third time and the place the pull is strongest, because job hierarchies are what every framework ships. Threads over processes: the work is NumPy and torch, both of which release the GIL where the time goes, and a process pool could not pass a loaded predictor at all. **Cancellation is cooperative, and there is no version of it that is not:** a queued job is dropped, a running one stops at its next `raise_if_cancelled()`, and **a running one with no checkpoint finishes anyway** — so the button means *stop at the next checkpoint*, which the GUI has to say, because the alternative is a button that appears to do nothing. Two tests pin the two halves side by side. Progress is **counts**, and `total = 0` means *cannot say*, which is the honest report from one opaque scientific call; a succeeded job's bar is forced to full, since 39 of 40 after the work is done says the work is not. One listener, **on the worker thread** — stated three times, because Qt widgets are main-thread only and M5's adapter must marshal. Failures are captured on the job rather than left unread in a `Future`, the default way a thread pool loses a traceback. **Found by the first test that ran an import under the runner:** `sqlite3` binds a connection to the thread that created it, so a project opened on the main thread was **unusable inside every job** — it would have arrived in M5 as a crash in a background task. Fixed with `check_same_thread=False` *and* one reentrant lock around every repository method, because statement-level safety is not enough when `save_analysis` writes three statements a second thread could commit half of. `import_images` is the first caller: progress per file, cancellation **between** files, and what was already imported stays imported. 16 tests, none of which sleeps to synchronise. Delta: **zero, golden byte-identical** |
| M4-T07 | Annotation entity + persistence | [x] Done 2026-08-12, **ADR-0044**. **The first data an operator makes rather than the application deriving it** — and the only thing in a project that cannot be recomputed, which is the whole argument. A **table**, two tasks after ADR-0042 sent the measurement table to a *file*, so the rule that separates them is written down: not "files versus database" but **does the shape vary, and is it derived?** A measurement table's columns depend on its producer and it can be re-run; an annotation is a fixed handful of numbers, edited **one at a time with undo behind it** (M4-T08), and irreplaceable — a JSON document rewritten per keystroke is a file lost mid-write, and an undo stack cannot address it. **One shape, the box:** it is what training consumes (M8, the only named consumer) and what a drag produces, while each extra geometry costs storage, an editor, a converter and tests for a reader nobody has written; masks stay deferred for the **third** time, because painting is M6 and a format written before its painter is written blind. **`source` (`manual` / `from_detection`) is not decoration:** training a model on boxes copied from its own output is self-confirmation, and a set that cannot tell them apart cannot avoid it. Annotations **cascade** with their image — a box pointing at an image the project no longer knows is not an annotation of anything — but `remove_image` now discards hand work, so the ADR puts an obligation on M6: `annotations_for` exists to be **counted before** a confirmation dialog, rather than a `force=True` that would put a UI decision in storage. Coordinates are **floats** (a drag is not on the pixel grid; rounding is the trainer's call), `Detection.bbox` stays integer, and a zero-area box is refused twice. **No use case** (ADR-0041, fourth application): the one with policy — adopting a run's detections for correction — arrives with the editor that triggers it. Schema **v3**, 20 tests. Delta: **zero, golden byte-identical** |
| M4-T08 | Undo/redo command stack | [x] Done 2026-08-12, **ADR-0045**. **M4's exit criterion "undo/redo proven on at least one mutating use case" is met**, against a real database rather than a stack of mocks. **The stack knows nothing but order** — it calls `do()`, keeps the object, clears the redo list, and never learns what a command *is*; one that knows about annotations would have to be extended for measurements and then for settings. Redo is `do()` again, not a third method. **Undo is a session, and persisting it is a promise deliberately not made:** replaying edits across sessions means replaying them against a directory that may have changed on disk, and ADR-0040 already established this application is not the only writer — *an undo history that can be silently wrong is worse than one that is honestly short*. The durable record is M4-T14's log, which is history rather than reversibility. **The plan was reversed by its own test.** It said a redo should insert a fresh row, because an `id=` parameter would be a back door; then `test_a_sequence_undoes_and_redoes_whole` — add a box, edit it, undo twice, redo twice — showed that a new id leaves every command stacked above pointing at a row that does not exist, so **undo would have been one command deep in practice, which is not undo**. `restore_annotation` puts a row back *as itself*, and it is a separate operation from `add_annotation` because creating and restoring are different acts and only one may choose an id; reclaiming the id is safe under a LIFO stack, since anything created after a deletion is undone before it. **A failing undo propagates and the history does not move** — swallowing it would leave the pointer describing a state that never existed, making the *next* undo silently wrong too. Three commands, all over annotations, because they are the only hand-made data whose loss is unrecoverable; imports are not undone (that is deleting the operator's files) and analyses are not (derived). No coalescing: the rule for "the same edit" comes from watching an editor M6 has not built. 18 tests. Delta: **zero, golden byte-identical** |
| M4-T09 | Autosave service | [x] Done 2026-08-12, **ADR-0046** — **closed by understanding rather than by code.** Architecture §4.5 scheduled autosave before there was any storage to autosave; the first job of this task was to check whether dirty state exists, and it does not. **Every mutating repository method commits before it returns** — eight write paths, eight commits — because ADR-0043's threading work made each write a short self-contained transaction, ADR-0045 decided the one deliberate piece of in-memory state (the undo history) is *not* persisted, and ADR-0042 writes the measurement file inside the call that records the run. An autosave service would be **a timer that flushes nothing**, and worse than useless: it would create the impression of protection where the protection actually lives, so the first person debugging a lost edit would inspect the timer instead of the write. **What ships is the proof rather than the service** — `tests/integration/test_durability.py` abandons repositories without `close()`, reads a row from a second connection while the writer is still alive, checks every write path individually, and **kills a separate process with `SIGKILL` between writes**, finding its rows intact. Two triggers are named that would reverse it: view state living only in the GUI (M4-T10's settings, the next task) and any write path that starts batching — the second of which turns these tests red, so the reversal would arrive with evidence. `Architecture.md` §4.5 corrected in the same commit, since it promised a service that will not exist. Delta: **zero — no production code was written at all** |
| M4-T10 | Settings service (application scope + project scope) | [x] Done 2026-08-12, **ADR-0047**. Two stores, split by **what the preference is about**: `~/.config/nanoscope/settings.json` (XDG, and on a Linux-only desktop application that is *the* convention rather than one of several) for what follows the **operator**, and a `settings` table in the project (schema v4) for what belongs to **the work** — so a copied project takes its choices with it. **Reads merge, project first; writes name their scope.** "Save this preference" without saying where is a question, not an instruction, and guessing is wrong in both directions: one project's choice leaking into every other, or a global preference hidden inside one directory. `set` defaults to `APPLICATION` and **raises** if `PROJECT` is asked for with no project open, rather than silently falling back. `scope_of(key)` exists so a dialog can say *"this project overrides your default"* instead of showing a value with no explanation. **Values are JSON, not strings** — a store returning text makes every reader parse it back and one gets it wrong, `"False"` being truthy — and `None` stored in a project is an *answer*, so the merge asks whether the project **has** the key. Neither store validates what a key means: a registry of known settings would be edited by every feature that adds one, including features in `gui/` this layer must not know about. The JSON file is written by **replacement** (`os.replace`), because a settings file truncated mid-crash is a preferences reset for somebody who did nothing wrong, and a corrupt one reads as empty rather than stopping the application from starting — and is **not** deleted, so an operator can still see what they wrote. `SettingsStore` is the second port to pay out `core/ports`'s promise, satisfied by two genuinely different implementations. 26 tests. Delta: **zero, golden byte-identical** |
| M4-T11 | CSV export service | [x] Done 2026-08-12, **ADR-0048**. ADR-0042 predicted this would be nearly free, since the measurement table is already a CSV. **The format was free; the export was not** — storage and export answer different questions, and *an export is a document for a person while the stored table is an index for the application*. Three differences, each a line a `shutil.copy` would not have. **Provenance in front:** `image`, `image_id`, `run_id`, `detector`, `mode`, `pixel_size_nm` on every row, because the stored table is *filed under* its run and the directory is its context, while a CSV on a desktop has none — a column of heights with no scan name is a column of numbers; in front rather than appended, since a spreadsheet opens on column A. `pixel_size_nm` is **empty, never 0**, when the scale is unknown, or a spreadsheet computes nanometres from a pixel count (ADR-0025 one layer out). **More than one run in one file**, because statistics across a dataset is the reason the measurements exist and assembling twelve files by hand is where an operator drops one. **A timestamped name**, since an export is a snapshot and silently replacing yesterday's loses work the operator believes they have; a caller's name is sanitised **in the repository**, where the write happens, because it arrives from a text field and a `/` would write outside `exports/`. **Nothing dishonest is written:** a detect-only selection **raises** instead of producing headers with no rows, an empty CSV being indistinguishable from *"we measured and found nothing"*, and a missing stored table fails loudly, because an export short by one scan of twelve is a wrong dataset that looks right. 10 tests over two analysed phantoms. Delta: **zero, golden byte-identical** |
| M4-T12 | `DeviceManager`: detect CPU / CUDA / ROCm / MPS, apply the selection policy | [x] Done 2026-08-12, **ADR-0049**, implementing ADR-0004 — **W8 closed** and a milestone exit criterion met, verified on the operator's own machine (*"NVIDIA GeForce GTX 1070 (cuda)"*, selected without a fallback). Before this, `grep -r cuda nanoscope/infrastructure/models` returned **nothing**: every inference this project ever ran went wherever torch felt like putting it. Three decisions came out of implementing it. **No torch means the CPU, reported rather than raised** — CI installs none on purpose, so a manager that raises on the import is one no test can run, and the machine still has a processor. **ROCm is told apart by `torch.version.hip`:** a ROCm build answers `torch.cuda.is_available()` with **True** and serves AMD cards through the same API, so a probe that trusts the function name reports a Radeon as CUDA — wrongness that survives for years because it never crashes. **A fallback says why, in a sentence** (*"CUDA was requested but no CUDA device is available — using CPU"*), which ADR-0004 asked for in those words and which is the easy part to skip, because the code works without it and a silent fallback is a forty-fold slowdown that reads as the application being slow. The preference order is a **stated convention with its reason**, not a fabricated measurement — there is no AMD card or Mac here — and it lives in one tuple. The probe is cached, with `refresh()` for the operator who fixed a driver without restarting. 14 tests drive it through a **fake torch module**, which is the only way the ROCm branch can be tested at all. `DeviceProvider` is the third port to pay out `core/ports`'s table, and `DeviceKind` — in `core` since M2-T02, unadopted since — finally has a resolver. Delta: **zero, golden byte-identical** |
| M4-T13 | Model registry + `ModelDescriptor` persistence (replaces hardcoded weight paths) | [x] Done 2026-08-12, **ADR-0050**, implementing ADR-0005 — exit criterion met, **without a weight file in sight**. W10 was two default arguments holding `"./checkpoints/best12x.pt"`: a relative path to a file nobody promises exists, with no version, no checksum and no record of what it was trained on. **The registry hands back factories, never instances:** constructing a detector loads weights, so instantiating on lookup makes *"what models does this project have?"* expensive — and impossible in CI, where no weights exist; a test resolves a model whose file is not there. **Weights may be shared, and the consequence is stated rather than prevented** — a path under `models/` is stored relative like every other, an absolute one to a 137 MB checkpoint is kept as it is, and `models` is the one table without the `NOT LIKE '/%'` check, because refusing would force duplicating gigabytes or lying about where the file is; a project carrying one opens elsewhere with that model unavailable. Identity is a **name somebody chose**, not a hash — an operator names their model, and the checksum answers the different question of whether the file still matches — and re-registering an id **replaces**, because retraining writes a new file under the name the configuration already uses. `provenance` is free text, since **provenance that must fit a schema stops being recorded**. **The device from M4-T12 arrives here**, closing the gap ADR-0049 named: `YoloDetector` gained a `device` parameter defaulting to `None` (ultralytics' own default, so nothing changes for a caller who does not choose), and no provider asks torch where to run. **`run_pipeline` is deliberately not rewired and no default changes:** its `if/elif` is what the `Detector` port removes, and doing it here would bundle a golden-covered refactor into a storage commit, which ADR-0010 forbids — so **W10 is not closed by this task, it is made closable**, with M5 named as the payer. Schema **v5**, 13 tests. Delta: **zero, golden byte-identical** |
| M4-T14 | Logging infrastructure: structured logs → file + SQLite, with rotation | [x] Done 2026-08-12, **ADR-0051** — **and the SQLite half is refused, with the argument written down.** ADR-0013 deferred that sink by name ("added when the database exists"); it exists, so this task had to write it or say why not. **The decisive objection is not performance: a log must not depend on the thing whose failure it records.** The most valuable lines this application will ever write are *"the schema is newer than this version"*, *"database.sqlite is corrupt"*, *"this directory is not a project"* — and a handler writing into that project's database has nowhere to put any of them. **A log that works only when everything works is not a log.** The rest would have sufficed alone: a write transaction per record contending for the very lock the records are about, a table growing with no rotation story, and a support request that starts "send me your database". **Two rotating files instead**, because there are two questions: `$XDG_STATE_HOME/nanoscope/nanoscope.log` for *what did the application do* (including every failure to open a project) and `<project>/logs/nanoscope.log` for *what happened to this work*, which travels with the directory and is what an operator attaches to a bug report. State, not config — a log is generated data nobody edits, so `~/.local/state`, not beside M4-T10's settings. Opening a second project **detaches** the first's handler, since one project's log continuing in another's file is worse than no project log. **JSON Lines:** `extra=` survives as *fields*, so M5's panel reads `image_id` instead of regexing it out of a sentence, and a value that will not serialise becomes its `repr` — a log line must never fail because somebody logged an object. Only `app/` attaches handlers, and they are **named**, so configuring twice replaces rather than duplicates. Rotation stated rather than agonised over: 5 MB, three backups, because an unbounded log is a disk-full bug that arrives months later blamed on something else. 12 tests. Delta: **zero, golden byte-identical** |
| M4-T15 | Headless end-to-end integration test of the whole layer | [x] Done 2026-08-12, **the last task of M4** — sixth exit criterion met, no ADR (a test that only exercises existing decisions makes none). **One long test on purpose**, which is normally a smell: here the *sequence* is the subject, and splitting it would produce fifteen more copies of what the per-task files already assert. It walks an operator's day in order — configure logging (T14), create a project (T04), state a preference (T10), register a model and resolve it (T13), select a device (T12), import a folder **as a job with progress** (T06), analyse both images (T05), annotate and undo (T07, T08), export (T11), close — and then **reopens and finds all of it**, which is the assertion that matters, because anything living only in memory disappears there. A second test states the same promise as *files*: a manifest that says `format_version: 1`, `images/`, `results/run_*/measurements.csv`, `exports/`, and **no `-wal` beside the database** — ADR-0003's argument that the operator owns their work, checked on the disk rather than through the API. **The Qt guard is written for a world where `gui/` exists:** "no Qt imported anywhere" is trivially true today and stops being true in M5, so the durable form is *nothing outside `gui/` imports it* — statically over every module and in a subprocess for transitive imports. Added while it is trivially true on purpose: a guard added after the first violation has already failed once. **Found by writing it, and filed as B-068:** `PipelineConfig`'s default `mode` is `"segment"`, so the most natural call in the project — `PipelineConfig(detector="log")` — raises *predictor must be provided*, and the default configuration is one **CI can never execute**. Not fixed here: changing a default changes what happens for every caller who omits it, including the notebooks, which is an operator's decision (PROJECT_RULES §4.5). 3 tests + 40 parametrised guard cases. Delta: **zero, golden byte-identical** |

---

## M5 — GUI shell ✅ (closed 2026-08-13)

**All nine tasks done, ADR-0052…ADR-0060, four of five exit criteria met and the fifth unverified
rather than unmet** — *"GUI smoke tests pass headless in CI"* needs a push and a run to read. Tests
828 → **1034**, of which **141 are headless GUI tests**; the golden did not move once. Milestone
summary in `docs/Progress.md`.

| ID | Task | Status |
|---|---|---|
| M5-T01 | Entry point, composition root, `nanoscope` console script | [x] Done 2026-08-12, **ADR-0052**. PROJECT_RULES §2.7 says all wiring happens in `app/` and nothing else constructs adapters — and until now **every caller of M4's eight components was a test that built them by hand**, which is fine in a test and wrong in an application: it is how two places end up deciding where the settings file lives. **The entry point works headless, today:** `nanoscope --project PATH` opens a project, prints what is in it and exits; `--devices` lists the hardware and says which would be chosen; `--gui` returns a sentence rather than an `ImportError`, because PySide6 is not a dependency yet. An entry point that only works once a window exists **cannot be run in CI**, and cannot be run by the operator who most needs it — the one whose project will not open. **A container, not a framework:** `Nanoscope` holds what is expensive or stateful and does the two things no single component can — resolve the device from the stored preference, and open a project as one act (repository, log, integrity report). `settings` is a *property*, rebuilt per access, because a cached merged view would answer for a project that has since been closed. **Our errors are messages; anything else keeps its traceback** — ADR-0030 built that distinction and this is the first user-facing surface able to use it. **ADR-0040's closing obligation is discharged at last:** the integrity report is *shown*, missing files named with the reminder that their rows are kept, untracked ones with the reminder that nothing was imported. `print` is permitted in exactly one module, scoped in `pyproject.toml` with its reason: **a command-line program's stdout is its user interface**, and a CLI that logs instead of printing has no output. Closing a project detaches its log and **clears the undo stack**, since commands referring to another project's rows are worse than no history. 16 tests. Delta: **zero, golden byte-identical** |
| M5-T02 | Main window: menus, toolbars, dockable panels, status bar, layout persistence | [x] Done 2026-08-12, **ADR-0053**. **W2 is no longer "no UI at all"**: `nanoscope --gui` opens a window that opens a project. First dependency the size of Qt, and the first line of code in `gui/` — the layer every rule so far was written to protect. **`QApplication` is created in `gui/launcher.py`, imported by `app/main.py` inside the `--gui` branch**, because M4-T15's guard fails if anything outside `gui/` imports Qt statically — and the guard is right: the headless entry point is the one CI runs, the one that works without a display, and the one an operator uses when a project will not open. **The window holds the container and constructs nothing** (Architecture §2.3): opening a project is `Nanoscope.open`, and `open_project(path)` is separate from `choose_project()` so the action is testable without a modal dialog. **The layout is a setting, not a `QSettings`** — Qt's `saveState`/`saveGeometry` base64-encoded into the **application** scope, because ADR-0047 already built a store with two scopes and a rule for choosing, and a window layout follows the *operator*, not the work; `QSettings` would be a second settings system, location and format for one value. An unreadable stored layout is **ignored with a log line**, since the answer to bytes Qt cannot parse is the default layout, not a refusal to start. Every dock **names the task that fills it** (M5-T04, T06, T08, and the centre M5-T05) and carries an `objectName`, without which `restoreState` silently drops it — a bug that appears once, in a user's config, months later. **An empty panel is a promise when it names its task and a bug when it does not.** Headless tests with `QT_QPA_PLATFORM=offscreen` set *before* Qt is imported, and no `pytest-qt` — a dependency for a six-line fixture. 14 tests; the real event loop was started offscreen and exited 0. **Found:** offscreen Qt clamps a window to an 800x600 virtual screen, which cost one confusing failure until the test asked for a size that fits — written into its docstring. Delta: **zero, golden byte-identical** |
| M5-T03 | Dark theme: design tokens + QSS, single source of colour truth | [x] Done 2026-08-13, **ADR-0054**, implementing ADR-0002. **"Single source of colour truth" is enforced by a test, not by review:** the stylesheet carries `@{token}` placeholders and **no colour of its own**, and a literal hex anywhere in `style.qss` turns a test red — the same argument as M4-T02's `CHECK` constraints, that *the rule and its enforcement ship together or only the rule does*. A placeholder with no token behind it **raises** rather than substituting an empty string, because an unstyled widget looks like a design decision and this one would be a typo. **Both a palette and a stylesheet, from one table:** QSS does not reach tooltips, dialog buttons, the text cursor or Qt's disabled states, and **Qt's default disabled colour on a dark palette is near-black on dark** — legible in the designer's head and nowhere else. **Contrast is a floor, not an opinion:** every text pair clears **4.5:1** (WCAG AA), recomputed from relative luminance by the test rather than asserted in a comment, and the floor applies to `TEXT_MUTED` too, because *"muted" must not quietly come to mean "unreadable"*. The formula is written out rather than depended upon — six lines against a package whose only job is to be trusted about arithmetic — and a test checks **the measure itself** (black on white is 21:1) first, since a contrast check that cannot fail is decoration. The palette is deliberately quiet with one cool accent: the thing worth looking at is a microscopy image, and a warm accent beside a warm colormap misleads. **Found while writing it:** the substitution ran over the stylesheet's own *comment*, which explains what an `@{token}` is — comments are now stripped first — and `@space_mdpx` parsed as one name, so placeholders are braced, because token names and CSS units are both lowercase letters. `uv build --wheel` was run and the wheel inspected: the stylesheet ships. 19 tests. Delta: **zero, golden byte-identical** |
| M5-T04 | Project explorer panel | [x] Done 2026-08-13, **ADR-0055** — and **ADR-0044's obligation is discharged where it was addressed**. That ADR decided annotations cascade with their image and then wrote a note to a task that did not exist yet: *`annotations_for` exists to be counted **before** the deletion, by a confirmation dialog that can say "this image has 12 annotations"*. Nothing could remove an image until this panel, so this panel pays it. **The dialog says the count and what survives** — three facts, all non-obvious: the annotations go, **they cannot be recomputed**, and **the file itself stays** in `images/` and will be reported as untracked. The third is the one an operator gets wrong in the other direction: a dialog saying only "delete?" leaves them believing the scan was deleted, and it is still there. **No dialog when there is nothing to lose:** a confirmation that always appears is one nobody reads by the third time, and then the one that mattered is clicked through as well — the guard is worth having *because* it is rare. A missing file is **marked, not hidden** (the integrity report is already in hand): a panel that lists an image whose file is gone without saying so is a panel that lies quietly. Removal is **not undoable**, which is ADR-0045's rule rather than an omission — undo holds edits, and reinserting a deleted image's rows, runs and annotations is a larger promise than a dialog. **`Nanoscope.repository` is now typed as the port**, because `gui/` may not import `infrastructure` and a container handing out a concrete adapter makes the rule unenforceable — mypy would infer the SQLite class into every panel and the import guard would never notice. 11 tests. Delta: **zero, golden byte-identical** |
| M5-T05 | Image viewer: zoom, pan, colormap, LUT range, scale bar, nm coordinate readout | [x] Done 2026-08-13, **ADR-0056** — **M5's second exit criterion met**, verified by rendering a characterization phantom into a real window (24 particles, a 100 nm bar, `x=100 y=50 px (200.0, 100.0) nm value=0.01413`). **The viewer shows what is in the file — raw, not flattened.** Every SPM tool flattens for display and a tilted map is harder to read, but flattening is an **analysis** with its own ADR whose output a *run* records: a viewer that silently flattens shows something the file does not contain, and an operator comparing it against a measured height compares two different arrays. **Rendering happens in `application`**, because the colormap lives in `infrastructure` which `gui/` may not import — and how a value becomes a colour is not a widget's decision. `QGraphicsView`, not matplotlib: zoom and pan are what a graphics view *is*, and a matplotlib canvas re-renders the figure on every wheel event, which is why scientific GUIs feel slow. **Three numbers make it a measurement rather than a picture:** a scale bar at a **round** length (a bar reading "137 nm" is one nobody can measure against by eye), a readout in **nm *and* px** with the value under the cursor, and **an honest absence** — no scale means no bar, no nm and the words "scale unknown", because this is the last surface that could have undone ADR-0025. The value window is the 2nd-98th percentile by default, since one hot pixel flattens the image to grey, with a full-range control because *"what am I clipping?"* must stay answerable. A refusal is a status line, not a dialog: the operator clicked a row, not a button labelled "load". **Two findings:** `load_afm`'s npy path leaked **numpy's own** `FileNotFoundError`, which PROJECT_RULES §3 forbids as a public contract and no caller catching `NanoscopeError` sees — now `MissingFileError`, fixed at the loader so every caller benefits; and the scan rendered as a **postage stamp** because `fitInView` ran before the widget had its final size, so the view refits on resize while it is still showing the whole scan and leaves a zoomed view alone. 21 tests. Delta: **zero, golden byte-identical** |
| M5-T06 | ViewModel layer + signal/slot contracts | [x] Done 2026-08-13, **ADR-0057** — **ADR-0055 §4's condition, met and paid.** That ADR declined a viewmodel and said when it would stop declining: *"when M5-T05's viewer needs the same selection, there will be two consumers and a reason"*. There are three — the explorer that makes a selection, the viewer that draws it, the Properties panel this task filled. **One viewmodel, not one per view:** `SessionViewModel` holds the open project, the selected image id and the loaded array; state only one widget can want (the colormap, the zoom) stays in that widget, because a viewmodel per view is four classes forwarding to one. **Intent goes in as a call, state comes out as a signal, and no panel is wired to a panel** — `explorer.image_selected.connect(viewer.show_image)` was one line at two panels and n² at n; every panel now subscribes to the session and to nothing else. **The image is loaded once**, for any number of panels showing it: a second read is a disk read per selection *and* two panels able to disagree about one scan. **The viewmodel holds no widget and opens no dialog**, and its tests construct none — ADR-0055's confirmation stays in the panel that asks it, since whether to ask and in what words is a view decision. **A refusal is one `failed(str)`** and the dialog stays where the button was: opening a project warns modally, failing to display a scan is a status line (ADR-0056, stated once instead of per panel). **The selection survives a failed load** — a scan whose file is gone is still the row the operator clicked, so *Remove* follows the selection and not the load. **A `QObject` because M5-T07 needs one:** ADR-0043's listener fires on the worker thread, and a queued signal is how Qt reaches the widgets' thread. **Three findings:** the new guard's own `startswith("nanoscope.app")` rejected `nanoscope.application` — the layer panels are *supposed* to import; **M5's fifth exit criterion had no owner and no check** and is now a test in `test_import_graph.py`; and opening a second project kept the first one's selection, ids being per-project. Found by *looking* rather than by a test: the panel read `Modality: afm`. 46 tests, **972** in the suite. Delta: **zero, golden byte-identical** |
| M5-T07 | Background job runner with progress and cancellation | [x] Done 2026-08-13, **ADR-0058** — **M5's third exit criterion met**, and **ADR-0043's marshalling obligation discharged** after being stated three times and owed since M4-T06. **The marshalling is `listener=self.job_changed.emit` and nothing else:** Qt queues a signal whose receiver lives on another thread, so the listener does one thing and the thread crossing is the *connection* — no `QThread` subclass, no `moveToThread`, and no second thread policy in the layer that must not have one. **Progress lives in the status bar**, because a modal progress dialog *is* the frozen window the criterion forbids; the bar shows `%v of %m`, the counts ADR-0043 §4 chose over a percentage, and `total == 0` becomes Qt's busy mode. **Cancel asks, and the button says so** — once pressed it reads *Stopping…*, since a button claiming "Cancelled" is one an operator watches do nothing for twenty seconds before deciding the application has frozen. **One job at a time in this window** though the runner takes two, and Open/Close/Import go dead while it runs, because `close_project()` closes the SQLite connection the worker is using. **The import asks two questions and invents neither:** modality, and a pixel scale whose minimum reads *unknown* and comes back as `None` — the first surface that *creates* rows is where a fabricated scale would enter (ADR-0025). **There was nothing long to run**, so the import action ships with the runner that makes it long. **Four findings:** a queued signal carries the **handle, not a snapshot**, so every "on finish" handler fires once per update ever emitted — the import refreshed and announced itself five times; **a cancelled import is a job that SUCCEEDED**, because `import_images` stops by *returning* its partial report, so `cancellation_requested` is the only record of the button; **two test files with the same basename collide at collection** (`tests/unit/test_jobs.py` and a new `tests/gui/test_jobs.py` — targeted runs passed, `make check` failed to *collect*); and a progress bar with its text off is an empty box, seen in the window rather than in a test. 26 tests, **998** in the suite. Delta: **zero, golden byte-identical** |
| M5-T08 | Log panel + user notifications | [x] Done 2026-08-13, **ADR-0059** — the **last placeholder dock** M5-T02 left, and the half of ADR-0051 nobody could reach: the log went to two rotating JSONL files, and an operator who wanted to know why an import refused a scan had to find `$XDG_STATE_HOME` in a file manager. **Records reach the panel as a Qt signal** — ADR-0058 §1 for the third time, and the repetition is the point: a job logs, so `Handler.emit` can fire on a worker thread. **What travels is a rendering, not the record**, which is deliberately the opposite of `job_changed`: a job handle is sent because a bar wants the *latest state*, while a log line is a **fact at a moment** — and `%`-style arguments are lazy (ADR-0013), so they must be applied on the thread that had the values. **`app/` attaches the handler** (ADR-0051), tagged so a second window replaces rather than duplicates, and the window detaches on close. **The panel is bounded and the file is the history:** `setMaximumBlockCount` is Qt's own ring buffer, levels are coloured from the tokens, and the text is HTML-escaped because the line most worth reading is the one with a repr in it. **A notification is a count in the dock's title** — *Log (3)* while it is not visible, cleared by looking at it; not a toast and not an auto-raised panel, because the two ways notifications fail are being missed and being resented, and `INFO` does not notify at all. **Four findings:** `logging.Handler.emit` **collides with `QObject.emit`**, so the multiple-inheritance version silently overrode a method of its own base (mypy caught it; a `type: ignore` would never have expired); **a window that is never closed leaves a handler pointing at a deleted widget**, printing `--- Logging error ---` into stderr — found from *another test file entirely*, where an assertion about tracebacks went red in a combined run; **an import wrote nothing into the project log**, noticed by reading the finished panel; and `isVisible()` is `False` for every widget in a window that was never shown, for the second time this milestone. 18 tests, **1016** in the suite. Delta: **zero, golden byte-identical** |
| M5-T09 | Settings dialog | [x] Done 2026-08-13, **ADR-0060** — the last task in M5's list, and it collects an obligation **M4-T10 wrote into a docstring**: `Settings.scope_of` exists *"to say **this project overrides your default** instead of showing a value with no explanation"*, and had had no caller since the day it was written. It also reaches a preference nothing could set — `DEVICE_SETTING` is read on every `select_device()`, and expressing it meant editing `settings.json` by hand. **Three rows, each with a reader that predates the dialog:** device (ADR-0049), default colormap (M5-T05), log level (ADR-0051). A settings dialog is where invented options accumulate, so the rule is that **nothing is offered which nothing reads**, with a test per row asserting the value lands in the store its reader already looks in. **It writes the operator's scope and says so**; project-scoped rows are not offered because this application writes none yet (M6's analysis parameters are the trigger). **A change takes effect where it can:** the level applies to the running process — an operator setting DEBUG is about to reproduce something — the colormap is the default for the *next* scan, since **the toolbar combo is this scan and the dialog is the default**, and the device waits for M6. **The stored level survives a restart and `--debug` beats it**, because somebody typing a flag is answering the question right now. **The keys moved to `application/settings.py`**: a settings key typed twice is a preference that silently does nothing on one side of the application. **Found:** the dialog showed the *project's* value in a control that writes the *operator's* — `preference()` merges project-first, so OK would have promoted one project's choice to every project, **ADR-0047's first failure mode in one screen**; caught by opening it against an overriding project, and hidden until then by the override note that made the screen look right. Also: `main` configured logging **before** constructing the container, so a stored level could not be read at all. 19 tests, **1034** in the suite. Delta: **zero, golden byte-identical** |
| M5-T10 | Headless GUI smoke tests (pytest-qt, offscreen) | [ ] |
| M5-T11 | Architecture lint rule: no `core.science` / `infrastructure` imports in `gui/` | [ ] |

---

## M6 — Analysis workflow in the GUI ✅ (closed 2026-08-13)

**Closed 2026-08-13** — all nine tasks, ADR-0061…ADR-0069, all four exit criteria, golden
byte-identical throughout. The exit criterion was *load → detect → segment → measure → export, entirely
through the UI*, and the rule that governs the whole milestone is in the roadmap: **the UI must not
introduce its own defaults.** Every parameter a panel offers has a default that already exists in
`core.science` or `application`, and where a panel disables a combination it does so **because the
capability matrix says so**, not because a widget repeated the rule.

| ID | Task | Status |
|---|---|---|
| M6-T01 | Preprocessing panel (flattening, substrate, live preview) | [x] Done 2026-08-13, **ADR-0061** — the first analysis step reachable from a window, and the one every later step stands on. It collects the toggle **ADR-0056 deferred by name** (*"a 'flatten for display' toggle is a later task with a checkbox and a name"*), and the name turned out bigger than the promise: not *flatten for display* but **which stage am I looking at** — raw, flattened, substrate, result, each named on screen. That is not a weakening of ADR-0056, it is what ADR-0056 said: *never show something the file does not contain **without saying so***. **The parameters are pass-through and the defaults are named once:** `min_size_nm` (ADR-0024), `manual_radius_px` (ADR-0014 — when given it *is* the radius) and `opening_scale` (ADR-0037's measured trade-off) reach the science with the values `run_preprocessing` already used, and the constants are named in `application` so a spin box shows the value it will actually get. **M6's rule is that the UI introduces no defaults**, and the compliance test is dull on purpose: an untouched panel and a bare call are compared **array for array**. **`preprocess_image` resolves the row the way `run_analysis` does** — record, path through the repository, and the scale the project recorded, which is the assembly M4-T05 found the D-07 family hiding in. **The preview is asked for, not live**, and runs as a job, because seconds of NumPy per keystroke is a UI that fights the operator; **it is not a result and is not stored** (M6-T09 owns persistence), and it **belongs to the scan it was computed from**. **Found:** the plan said five stages and the entity carries four (no plane-only array, and adding one would change what a *run* records); the AFM format helper was about to be copied a third time and moved to the module that loads AFM files; the stage label was **clipped to "result (flatte…"**, seen in the window rather than by a test; and the `gui/` import guard refused `core.science.DEFAULT_OPENING_SCALE` on its first real chance. 26 tests, **1056** in the suite. Delta: **zero, golden byte-identical** |
| M6-T02 | Detection panel driven by the capability matrix | [x] Done 2026-08-13, **ADR-0062** — **M6's third exit criterion met**, and verified in a window. `detector_options(modality, frameworks, has_predictor)` answers *what may be offered*, beside the matrix it reads; the panel renders it and decides nothing, so its combos hold the matrix's own strings rather than any it typed (PROJECT_RULES §2.5, and D-19 is what the other outcome looks like — the deleted React client kept a copy of this matrix and the copy had drifted). The matrix has had **one caller since M2-T10**, a validator that runs *after* a request is assembled; this is the direction that stops an invalid request being expressible. **An unavailable entry is offered, disabled and explains itself** — *"no ultralytics model is registered in this project"*, *"segmentation needs a loaded predictor"* — because "you need to register a model" and "this application cannot do that" are different sentences; the AFM-only mode is **absent** rather than greyed, since the matrix has no such row and a greyed one would be the widget restating the rule. **Running it stores a run** with its detections and its measurement table (ADR-0042): M6-T01's preview was explicitly not a result, this explicitly is. **The preprocessing parameters travel with it** as one `PreprocessingParams` the session holds. **Found:** the name guard failed on this panel's own **docstring**, which quoted the rule including the names it is about (the wording changed, not the guard); **`run_analysis` preprocessed with its own defaults**, so a scan previewed at one opening scale would have been analysed at another with nothing saying so; `QComboBox.model()` is typed as the abstract base, so disabling an entry Qt's own way needs a `cast`; and `detector_options` needed a **second table** for which framework a detector requires, recorded as a negative consequence. 18 tests, **1074** in the suite. Delta: **zero, golden byte-identical** |
| M6-T03 | Detection overlay rendering on the canvas | [x] Done 2026-08-13, **ADR-0063** — M6-T02 ended with *"30 detection(s)"* in a status bar and a scan with nothing on it. **The shape is the one the detection carries:** a box when `bbox` is present, a circle of `radius_px` when it is `None` (ADR-0031), because an invented box around a circle is the substitution ADR-0028 removed from `confidence` one field over. **The overlay lives in the scene, in pixel coordinates**, so the view's own transform keeps every item on its particle at any zoom, with a **cosmetic** pen so a 2 px circle stays a one-pixel outline at 32x instead of becoming a blob. **The run shown is the newest stored one for the selected image** — `runs_for` had existed since M4-T05 with **no reader at all**, so a scan analysed yesterday showed nothing today. One colour, not the colormap's: a confidence ramp would be a second scale beside the data's, and half the detections have no confidence to colour by. **Found:** the count label was the seventh widget in the viewer's control row and **clipped mid-word** in a real window, so it rides on the toggle now (`Detections (30)`); and **the circle branch has no producer today** — the blob detector synthesises a bbox (M3-T24, M4-T05), so every detection currently draws as a box, which is recorded rather than papered over. 10 tests, **1084** in the suite. Delta: **zero, golden byte-identical** |
| M6-T04 | Segmentation panel + mask overlay | [x] Done 2026-08-13, **ADR-0064** — M6-T02 disabled every `segment` row with a sentence naming this task, and this is the task that makes it true. **The composition root builds the predictor, lazily and once**: a segmentation model in the open project → `resolve()` → the factory, with the device `select_device` would choose — the last unwired half of ADR-0050, whose `resolve()` had no caller outside its own tests. **"Can this project segment?" is answered by a registered model, not a built one**, because ADR-0050 made the registry cheap so that filling a combo box reads no weights; the predictor is constructed **inside the job**. **There is no new panel** — the detection panel already offers modes from the matrix, and a second panel would carry its own detector and mode choice, which is ADR-0062's own case one task later: *the task list asked for a panel and the panel already existed*. **The mask parameters are not offered**, and the reason is structural: `PipelineConfig`'s fields for them are named after the framework, **the golden records that class's field names**, and a widget setting them would have to write the name §2.5 forbids — the rename is a golden-moving change and gets its own commit (ADR-0010). **The masks ride on the run in memory only**, empty on everything the repository returns, because ADR-0042 did not persist them and an overlay drawn from a re-read run would show what the project cannot restore. **A mask is an outline, not a fill** (the pixels it covers are the measurement), built from row spans because `gui/` may not import `skimage`, and its toggle **disappears when there are no masks**. Exercised by a **stub predictor registered through the registry** — there are no weights here or in CI, and the alternative is not testing the path at all (M3-T14's precedent). 11 tests, **1095** in the suite. Delta: **zero, golden byte-identical** |
| M6-T05 | Measurements table with two-way canvas selection | [x] Done 2026-08-13, **ADR-0065** — **M6's second exit criterion met**: a row selects the particle, a click on the particle selects the row, and **neither widget tells the other** — both ask the session and both are told by it (ADR-0057, where *"and vice versa"* is the case that would bring widget-to-widget wiring back). **The link is a coordinate, not an index:** the table is a *subset* of the detections, since a height that is not a number never reaches it (ADR-0033), so row *n* is not detection *n*. **And it has to be a coordinate**, because `particle_id` means two different things — the baseline producer writes the blob's index, the segmentation producers write the index of the row being appended, which renumbers after every drop. One column, two meanings, in the schema ADR-0031 built to stop that: filed as **B-069** rather than fixed here, because changing what a producer writes is a stored-data change with its own commit (ADR-0010). **A click is a release that did not move** (three pixels), because this view drags to pan. **The columns are the producer's own** — renaming them for display would be a second vocabulary beside the exported CSV's — and **a run that measured nothing says so** instead of showing an empty grid (ADR-0042). **Found:** tabbing the Measurements dock in front of the Log dock changed what an M5-T08 test meant, because a dock behind a tab is **not visible** — which is exactly the semantics the unseen-warning count wants; and the layout test's absolute size assertion became order-dependent once the table's minimum width pushed the window's past 640, so it now compares against what the first window actually got. 10 tests, **1108** in the suite. Delta: **zero, golden byte-identical** |
| M6-T06 | Statistics panel + histograms | [x] Done 2026-08-13, **ADR-0066** — M6-T05 put thirty rows on screen, and nobody measures thirty particles to read thirty numbers. **`application` computes and the widget renders:** a panel that computes a mean is a second place deciding what a mean is, and the first `NaN` makes the two answers differ. `count` is the number of **finite** values; the spread is the **sample** standard deviation and is **undefined for one particle**, printed as absent rather than as a zero. **The bins come from a named rule** — numpy's `auto`, the larger of Sturges and Freedman-Diaconis — because a fixed 20 is an invented parameter and *the shape of a histogram is the claim it makes*. **The bars are painted by Qt**: matplotlib is `infrastructure` and `gui/` may not import it, so the binning is numpy's in `application` and what is left is rectangles — no `QtCharts` module for a chart nobody clicks. **The columns are the table's own**, with identifiers excluded (an averaged `particle_id` is a number with no meaning) and all-`NaN` columns not offered. **Found — and it corrected the panel's own wording:** an unscaled scan **keeps its heights**. The first note said an unknown scale means "no physical columns" and the test written to prove it failed: a height is calibrated by the **z** axis, a radius comes from the pixel size, so losing the lateral scale costs the sizes and nothing else. Also: `np.issubdtype` **raises** on the pandas extension dtype every measurement table carries (`method`), rather than answering "not numeric". 14 tests, **1127** in the suite. Delta: **zero, golden byte-identical** |
| M6-T07 | CSV export UI | [x] Done 2026-08-13, **ADR-0067** — the last step of M6's first exit criterion. `export_measurements` had been in `application` since M4-T11 with **tests as its only callers**: the operator's way to get a table out was a Python prompt. **Two scopes, named in the menu** — *this run* and *every run in the project* — because ADR-0048 built the second deliberately (*statistics across a dataset is why the measurements exist*) and one item that silently meant one of them is one somebody uses wrong exactly once, on the export they then analyse. **The file goes into the project's `exports/` with the name the use case chooses**, not through a file dialog: an export is part of the project (ADR-0003) and is timestamped so today's does not replace yesterday's, and what the window owes is **where it went**. **Nothing to export is the use case's own sentence** — a detect-only run raises rather than writing headers with no rows, and the action is *not* disabled for it, because a disabled control says less than the refusal and teaches nothing. Runs as a job, the runner's fourth consumer. **Found:** two test files with the same basename broke collection **for the second time** (`test_export.py` against the integration one, exactly M5-T07's `test_jobs.py` trap) — renamed, and **this time the lesson ships as a guard**: a test walks `tests/` and fails on any basename clash, because a lesson that lives only in a Progress entry is one the project gets to learn twice. 7 tests + the guard, **1135** in the suite. Delta: **zero, golden byte-identical** |
| M6-T08 | Multi-image navigation within a project | [x] Done 2026-08-13, **ADR-0068** — the workflow M6 assembles is *look at a scan, run it, read the numbers*, and the real version is doing it to forty in a row; every one of them cost a trip back to the explorer. **Navigation is `select_image` with a different id** — not a second mechanism, and the id comes from the project's own order (ADR-0057's one place to ask). **It does not wrap:** taking an operator from the fortieth scan back to the first without saying so means a batch review quietly starts again, and *"did I look at all of them?"* is exactly the review that must not lie — the actions go dead at the ends. **The status bar says "3 of 40"**, permanently, because half of navigating is knowing whether there is anywhere left to go. **The explorer follows with its signals blocked**, since a panel listing the images while a different one is on screen is a panel that lies, and setting the row would otherwise ask the session for the selection it just announced — the loop M6-T05 met on the measurements table. **The zoom does not survive the move** (scans differ in size and a held zoom shows a corner); *keep the view* is a feature with a control and a name, not a default. **Found:** `next_action` was **enabled with no project open**, because the window set its actions from signals and never from its own initial state — a test asked, and the window now starts in the state the session implies. 10 tests, **1145** in the suite. Delta: **zero, golden byte-identical** |
| M6-T09 | Result persistence and restoration across restart | [x] Done 2026-08-13, **ADR-0069** — **M6's fourth exit criterion met**, and most of it was already true by construction, which made this a task about **proving it and finding what does not** (M4-T09's shape). **The proof ends the process's grip on the project:** the container is closed and the repository connection with it, then a **new** container and a **new** window open the same directory and find the run, its detections, its measurement table, its overlay and its statistics — anything less proves a cache. **Older runs are reachable**, which is the gap this found: three analyses of one scan leave three rows and the window could reach exactly one, so a sweep of three parameter sets showed only the last — *results persist* satisfied on a technicality. A **run selector** now lists them, and choosing one goes through the same `session.select_run` the rest of the selection lives in. **A fresh selection still shows the newest**, because remembering which run an operator was looking at, per image, across restarts is a project-scope preference nobody asked for. **What does not survive says so:** a restored segmentation run has its detections and **no masks** (ADR-0042 never stored them, ADR-0064 kept them in memory), and an empty overlay reads as *"segmentation found nothing"* — the panel says *"its masks were not stored and cannot be redrawn"* instead. **Found:** the first draft asked `run.mode == "segment"` and **M6-T02's guard caught it** — which modes make masks is the matrix's answer, not a literal in a widget. Also recorded as a negative consequence: the selector lists runs by id, mode and detector, so **a sweep of three opening scales looks like three identical rows** — a run does not store the `PipelineConfig` it used. 6 tests, **1151** in the suite. Delta: **zero, golden byte-identical** |

---

## M7 — Annotation & metrology tools (in progress)

**Opened 2026-08-13.** M4-T07 made an annotation a row **because it cannot be recomputed**, M4-T08
built undo around it, and nothing in a window has ever drawn one. The milestone's own rule, from the
roadmap: manual measurements are a **new output**, and they get their own tests.

| ID | Task | Status |
|---|---|---|
| M7-T01 | Annotation layer model + rendering pipeline | [x] Done 2026-08-13, **ADR-0070** — M4-T07 made an annotation a row **because it cannot be recomputed**, M4-T08 built undo around it, M5-T04 counts them before a deletion, and **nothing in a window had ever drawn one**: the most expensive data in a project was the only data with no representation on screen. **An annotation is not a detection and does not look like one** — its own colour, its own toggle and count, drawn **above** the detections because it is what the operator is working on and what a click should reach first when the tools arrive. **The two sources are visibly different:** `manual` is solid, `from_detection` is dashed, because ADR-0044 made that distinction load-bearing for training — *a model trained on its own output is confirming itself* — and a screen that draws them alike undoes it in the one place an operator would have noticed; a test asserts **every** source has a style, since one without raises while drawing. **The label is the operator's own text and does not scale** (`ItemIgnoresTransformations`): a box with no label is a rectangle, and a label that fills the screen at 32x is one nobody can read. **This task mutates nothing** — no command reaches the stack, so M7-T02's drawing and M7-T07's editing stay testable. **Found:** M6-T09's `find(...)` call **shadowed the loop variable `row`** in the measurements panel, taking mypy from 6 to 8 — the commit before this one claimed "unchanged at 6" and was wrong; fixed here, and mypy is back at 6. 11 tests, **1162** in the suite. Delta: **zero, golden byte-identical** |
| M7-T02 | Point and box tools | [x] Done 2026-08-13, **ADR-0071** — the first surface in this project where an operator **makes** data rather than asking for it to be computed. **The point tool is not built, and the reason is the shape:** ADR-0044 stores one shape, the box, and refuses a zero-area one twice — in the repository and in a `CHECK` — so a point tool must invent an extent, and a *point size* control is that invention wearing a label. ADR-0044 wrote the condition for revisiting it itself (*a shape that has a reader*), and a point has none: M8's dataset builder consumes boxes. Closed with the argument, the way M4 closed three of its own and M6-T04 closed its panel. **Drawing suspends panning, visibly** — a tool that draws *and* pans on one gesture does the wrong one half the time. **The label comes from a field**, because forty particles through forty dialogs is a feature nobody uses twice, and **an empty one is refused with a sentence** while **a drag under three pixels is discarded silently**: a wrong label is worth telling somebody about, a slipped click is not. **Every box goes through the command stack**, so M4-T08's promise — *redo puts the **same row** back, because a fresh id leaves every command above it pointing at nothing* — finally has a caller outside its own tests, and the Edit menu is labelled by what Undo would take back. **Found:** nothing told the window the *history* had moved — the Undo item stayed dead after the first box until `annotations_changed` was wired to it, which works only because every command today mutates annotations; the first that does not needs its own signal (M7-T08). 17 tests, **1179** in the suite. Delta: **zero, golden byte-identical** |
| M7-T03 | Polygon tool | [x] Done 2026-08-14, **ADR-0072** — **schema v6**, the first change to the schema in three milestones, and the other side of M7-T02's argument: a point was refused for having no extent and **no reader**, and ADR-0044 wrote the condition for revisiting the shape decision itself — *a shape a box cannot express, with a reader*. A particle that is not a rectangle is the ordinary case in this science. **The outline is stored beside the box, not instead of it:** one nullable `points` column, and a polygon's `x1…y2` are its **derived** bounding box — so every reader that consumes boxes keeps working unchanged (M5-T04's confirmation, M7-T01's layer, M8's detection dataset), `points IS NULL` still means *a box drawn as a box*, and **nothing migrates**, because that is exactly what the existing rows already mean. **The box is derived, never typed in**, so a polygon and its bounding box cannot disagree; **fewer than three vertices is not an outline** (two are a line, one is the point ADR-0071 declined); **JSON in a column rather than a second table**, because an outline is read and written whole and never queried by vertex; and **the undo stack carries it** — a redo that restored the box and dropped the outline would silently redraw the operator's work as a rectangle. The canvas draws the outline *and the sketch while it is made*, because an outline nobody can see until it is finished is one they draw twice. **Found:** v6 is the first migration that **alters** a table rather than adding one, and the schema-history helper's model — *drop the tables a later step created* — left the column behind, so re-running the step answered `duplicate column name: points`; it now undoes columns too, from a second map with the same guard over it. 13 tests, **1192** in the suite. Delta: **zero, golden byte-identical** |
| M7-T04 | Brush tool (mask painting) | [x] Done 2026-08-14, **ADR-0073**, **schema v7** — the third shape, and the first that is not a handful of numbers. PROJECT_RULES §5 decided where it goes before the task existed (*no mask bitmaps in the database; masks are files, the database stores paths*), and `annotations/` has been reserved for them since M4-T01 — so the decisions here are about **storage and honesty**, not about the brush. **The mask is a file and the row keeps its path**, written *after* the row because the id is the file's name (M4-T05's own sequence); **PNG rather than `.npy`**, because a mask an operator painted is a picture of their judgement and a format every image viewer can open is worth more than a few bytes. **The box is derived from the painted pixels**, so the three shapes now agree on one rule: whatever a reader wants as a box, the repository computes from what was actually drawn. **Undoing an add removes the row and leaves the file** (ADR-0040's rule, third application: forgetting and deleting are different decisions), and a redo points the restored row at the same file. **Nothing is painted into the scan** — the brush paints into a mask of its own, because a tool that edited the data an operator is measuring would be the worst version of this feature. **A stroke that painted nothing stores nothing**, quietly; **a missing mask file is a refusal, not an empty mask**, because an empty one reads as *the operator painted nothing*. Recorded as a negative consequence: `check_integrity` does not look at `annotations/` yet, so a mask deleted behind the application's back is found when something tries to draw it. 15 tests, **1207** in the suite. Delta: **zero, golden byte-identical** |
| M7-T05 | Measurement line + distance tool | [x] Done 2026-08-14, **ADR-0074**, **schema v8** — the first output in this project that **no algorithm produced**, which is why the roadmap's risk line for M7 says manual measurements get their own tests. **It is not an annotation**, for the reason that refused the point: a line has no area, and ADR-0044's shapes are refused without one, twice. **The word "measurement" is taken** — `measurements.csv` is what an analysis *run* produces (derived, re-runnable, shaped by its producer), and calling both by that name makes *where are the measurements?* a question with two answers; the table is **`rulers`**. **One table, two tools:** a profile line is the same geometry read differently, so the row carries a `kind` and the migration happens once. **The length is computed, never stored** — a stored copy is a second answer waiting to disagree with the endpoints — and the arithmetic lives in `core/science/metrology.py`, because two points and Pythagoras in a widget would be the first science in `gui/` in seven milestones. **Without a scale there is no length in nanometres:** pixels, and the words *scale unknown* — ADR-0025 at the first surface that **produces** a physical number rather than reading one, with a non-positive scale raising because *wrong* and *absent* are different. **Found:** undo reloaded annotations only, so **undoing a ruler left the line on the canvas** — M7-T02 could wire the window's Undo label to `annotations_changed` while every command mutated annotations, and this is the command that ended that; both signals now mean *the history moved*. 17 tests, **1231** in the suite. Delta: **zero, golden byte-identical** |
| M7-T06 | Profile line + height-profile plot | [x] Done 2026-08-14, **ADR-0075** — **M7's third exit criterion met**, and the first job was to read the reference it names. `notebooks/afm_gold_nanoparticles.ipynb` §5.1 does exactly one thing: `z_flat[y_i, x_i-half : x_i+half]` — **a horizontal row slice, no interpolation**. So **the criterion covers one case**, and that case is asserted as **equality** (`array_equal`, not `allclose`); an arbitrary line has no notebook counterpart, so it is an extension with a rule of its own. **Bilinear**, because a diagonal profile made of nearest-neighbour steps is a picture of the sampling rather than of the sample — proven by the one assertion nearest-neighbour cannot pass: a line half-way between a row of 0 and a row of 10 reads 5 all the way along. **One sample per pixel of length plus the far end**, which is what makes the equality exact rather than approximate; **clamped at the edges rather than extrapolated**, because inventing values beyond the data is a measurement of nothing; and **validated like every other numerical entry point** (`ensure_height_map`, ADR-0030's funnel at its fifteenth site). **The profile names the stage it measured**, because a raw map and a flattened one give different numbers and a measurement whose provenance is a checkbox somebody set four clicks ago is not one anybody can defend. `ruler.kind` from M7-T05 gets its second reader, as designed. 19 tests, **1250** in the suite. Delta: **zero, golden byte-identical** |
| M7-T07 | Manual add/edit/delete of detections | [x] Done 2026-08-14, **ADR-0076** — and **reading the task properly was most of the work**: *a detection is not edited*. A stored detection is what a detector produced in a run (ADR-0042), and an operator deleting one makes the run describe an analysis that never ran — the project would lose its only honest answer to *what did the detector actually find?*, which is the question M3-T15's harness exists to ask and the number M8 trains against. **Correcting the machine is adopting its answer, and the adoption is marked:** ADR-0044 built `source` for this and said why — *a model trained on its own output is confirming itself* — and **`AnnotationSource.FROM_DETECTION` gets its first writer**, four milestones after it was defined. A blob detection has no `bbox`, so the circle becomes the square bounding it, which is ADR-0044's own stated conversion. **Adoption is one click and adopting everything is one more**, because reviewing forty detections means keeping thirty-eight and fixing two. **Deleting one box asks nothing** — M5-T04's confirmation exists because removing an *image* destroys hand work, and a dialog for one box with `Ctrl+Z` in the same menu is the one nobody reads by the third time. **`UpdateAnnotation` and `RemoveAnnotation` get their first callers** outside their own tests, and editing acts on the selection the canvas already had — a click reaches the annotation layer first because M7-T01 drew it on top **for exactly this**. 14 tests, **1264** in the suite. Delta: **zero, golden byte-identical** |
| M7-T08 | Undo/redo wired through every tool | [x] Done 2026-08-17, **ADR-0077**, filed **B-070** — every tool since M7-T02 already went through the stack, so the task was an **audit**, and an audit's worth is the gaps it finds. **Two of the three were written down by the tasks that made them.** M7-T02 wired the Undo menu to `annotations_changed` and recorded that this held *only* while every command mutated annotations; M7-T05's ruler was the first that did not and got a second signal beside the first; **a third would have been the same mistake again**, so `history_changed` is now what the menu listens to — emitted when a command runs, when the history steps, and when a project opens or closes and takes the stack with it (ADR-0045). **One gesture is one undo:** `adopt_all_detections` ran one command per detection, so ADR-0076's *one click* needed forty `Ctrl+Z` to reverse — which nobody spends, so what they do instead is close the project. `Composite` makes it one entry, and **a child that fails takes back the ones already done**, because `CommandStack.run` promises a command that raised is not on the history and half a batch is an edit nothing can reverse. **Undo goes to the work it undoes:** the history is per project and the annotation layer is per image, so undoing an edit made on another scan removed a row nobody could see and left the window unchanged — each command now says which image it edited, `UpdateAnnotation` and `RemoveAnnotation` reading it off the row they captured rather than from an argument that could disagree, and **the stack still never asks**. The audit itself ships as a table of ten actions, each *on the stack* or *not, and why* — because *"is everything undoable?"* is otherwise re-answered by grepping once a milestone. **Found:** `remove_ruler` has had exactly one caller since M7-T05, `AddRuler.undo`, so the third of five rulers cannot be removed at all and the only mechanism that reaches it also takes back everything drawn since — filed as **B-070** rather than smuggled in, because it needs the canvas selection annotations got in M7-T07 and rulers never did. 18 tests, **1282** in the suite. Delta: **zero, golden byte-identical** |
| M7-T09 | Annotation export/import in a training-ready format | [x] Done 2026-08-17, **ADR-0078** — **M7's fourth exit criterion**, and the shape of the task is the mismatch it has to be honest about: a row here carries an outline, a painted mask, a note, an id, two timestamps and the `source` that says whether a person or a detector produced the box; a label file carries a class index and four numbers. **The format is the trainer's** — `labels/<stem>.txt`, `class cx cy w h` normalised, and a `classes.txt` whose line numbers *are* the indices — because that is what M8-T03 will train with and what labelImg, CVAT and Roboflow read and write, which is what makes export and import **one** decision rather than two formats that drift. **`data.yaml` and the train/val split are not written here:** a split is a dataset decision — how much to hold out, stratified by what — and writing one means M8-T02 arrives to find its central decision already made by the task that happened to write the labels first. **What the format cannot carry, the caller chooses.** An outline exports as its bounding box, which is **already what the row stores** beside it (ADR-0072), so nothing is lost that the row did not lose first; `source` is the exception, because ADR-0044 made it load-bearing — *a model trained on its own output is confirming itself* — so the scope is **two named menu items** (ADR-0067's rule, one milestone on) and **the scope is in the directory name**. **An import is told what it cannot know** — a `.txt` says nothing about who drew the box, so it asks, the way M5-T07's dialog asks modality and scale and *invents neither*, and the directory it came from goes into the note. **An import is one edit on the main thread** — one `Composite`, so two hundred labels are one `Ctrl+Z` (ADR-0077's second caller, one task later), and not a job, because the command stack is single-threaded **by decision** (ADR-0045) and a worker pushing onto it is two people editing one history. A class index no list has, a coordinate outside `[0, 1]` and a line that is not five fields are **refusals**; a label file naming no image here is **reported** (ADR-0040); a box that ran off the scan is **clamped**, because a drag past the edge is ordinary and the part on the image is the part that was measurable. **Found:** two exports a second apart landed in **one directory**, mixing a hand-drawn set with an everything set in the file an operator would then have trained on — caught by the test that asserted the filter, and fixed in the name; and **the M6-T02 name guard caught the viewmodel saying *YOLO***, which is `application`'s word and not the window's. 19 tests, **1305** in the suite. Delta: **zero, golden byte-identical** |
| M7-T10 | Measurement semantics documented: height, diameter, distance, aspect ratio | [x] Done 2026-08-17, **ADR-0079**, filed **B-071** and **B-072** — the last task of M7, and the one the milestone earned: M7-T05 and M7-T06 added outputs *no algorithm produced*, M7-T09 sent the boxes to a trainer, and M8 will compare a model against numbers whose definitions lived in four producers' docstrings. **A column called `height_nm` is not a measurement until somebody writes down what it is the height of.** `docs/Measurements.md` covers units and conventions, every column, both hand tools, and what is dropped and why — and **a test holds it to the schema** (M5-T03's rule applied to prose: every column `measurement_columns()` can declare must be named there, and the document may name none it cannot), proved by adding a `volume_nm3` column and watching the suite go red on exactly that name. **`height_nm` is one column and two estimators:** both compute *peak − baseline*, but the baseline producer's peak is over a **circular mask built from the detector's sigma** and falls back to the **global substrate median**, while SAM2's is over the **eroded real mask** and **skips the particle** instead — so `baseline_source` is always `ring` on that path. **`area_px` is sharper:** a disk drawn from a detector's estimate on one path, a measurement of the particle on the other; comparing them compares a guess against a result. **The project reports radii, not diameters** — the task's own title says *diameter* and there is no such column, only the **equivalent-area** `radius_px`/`radius_nm` and the detector's `detector_radius_nm`; the conversion is documented rather than the schema growing a second answer that can disagree (ADR-0074's rule, applied to a table). **Found by reading the code for the document:** `aspect_ratio` returns **1.0 — the value that means *a circle*** — for a mask with no minor axis, so a one-pixel-wide line is reported perfectly round (measured), and `circularity` returns **12.57** for a single pixel through the same `... else 1.0` guard, while a *real* digitised disk of radius 10 scores **0.916** rather than 1.0. Both are **a constant standing in for an undefined value** — the class ADR-0025 removed for scales and ADR-0033 for NaN heights — and both are **filed, not fixed** (**B-071**), because they change stored numbers and that needs operator sign-off and its own commit (PROJECT_RULES §4.4, §4.5); **B-072** covers the skimage properties deprecated on the same line. The block is only reachable through the SAM2 SEM/TEM path, which is outside the gate, **which is why it survived to M7**. 22 tests, **1327** in the suite. Delta: **zero, golden byte-identical** |

---

## M8 — Training module

| ID | Task | Status |
|---|---|---|
| M8-T01 | `TrainingProvider` port: job contract, status, metrics, artifacts | [ ] |
| M8-T02 | Dataset builder: annotations → YOLO dataset with a train/val split | [ ] |
| M8-T03 | `LocalTrainingProvider` (ultralytics), device-aware via `DeviceManager` | [ ] |
| M8-T04 | Training-run persistence: config, metrics, artifacts, provenance | [ ] |
| M8-T05 | Training UI: configuration, live metrics, cancellation | [ ] |
| M8-T06 | Model management UI: import, register, activate, compare | [ ] |
| M8-T07 | `RemoteTrainingProvider`: protocol, client, contract tests | [ ] |
| M8-T08 | Model evaluation report using the M3-T15 harness | [ ] |

---

## M9 — Release readiness

| ID | Task | Status |
|---|---|---|
| M9-T01 | Rewrite `README.md` as a product README; archive stale documents | [ ] |
| M9-T02 | User manual in `docs/` | [ ] |
| M9-T03 | Linux packaging (AppImage or equivalent) + desktop entry | [ ] |
| M9-T04 | Diagnostics bundle for bug reports | [ ] |
| M9-T05 | Performance pass on large scans | [ ] |
| M9-T06 | v1.0 release checklist and tag | [ ] |

---

## Counts

| Milestone | Tasks | Done | Blocked |
|---|---:|---:|---:|
| M0 | 8 | 8 | 0 |
| M1 | 11 | 4 | 0 |
| M2 | 16 | 0 | 0 |
| M3 | 19 | 0 | 3 |
| M4 | 15 | 0 | 0 |
| M5 | 11 | 0 | 0 |
| M6 | 9 | 0 | 0 |
| M7 | 10 | 0 | 0 |
| M8 | 8 | 0 | 0 |
| M9 | 6 | 0 | 0 |
| **Total** | **113** | **12** | **3** |
